# finding 14 / P42: the numpy MAJOR version changes GroupKFold's tie-breaking, so a
# numpy-1.x install builds a DIFFERENT data/folds_grouped/ split that no committed
# result is comparable with. Same pin as config/environment.yml.
numpy>=2.0,<3
scipy>=1.5.0
scikit-learn>=0.23.0
gensim>=3.8.0
git+https://github.com/epfml/sent2vec.git
Cython>=0.29.0
nltk>=3.9.0
matplotlib>=3.3.0
