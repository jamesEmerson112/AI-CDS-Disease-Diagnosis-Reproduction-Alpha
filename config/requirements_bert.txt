# Bio_ClinicalBERT Requirements
# Install with: pip install -r requirements_bert.txt

# Core BERT and Transformer dependencies
sentence-transformers>=2.2.0
transformers>=4.21.0  
# >=2.3 is the numpy-2-compatible line; below it torch is built against the
# numpy 1.x ABI and will not import beside the numpy pin below.
torch>=2.3
accelerate>=0.12.0

# Scientific computing (likely already installed)
# finding 14 / P42: the numpy MAJOR version changes GroupKFold's tie-breaking, so a
# numpy-1.x install builds a DIFFERENT data/folds_grouped/ split that no committed
# result is comparable with. Same pin as config/environment.yml -- and the reason
# torch is floored at 2.3 above.
numpy>=2.0,<3
scipy>=1.7.0
scikit-learn>=1.1.0

# Visualization (existing dependencies)
matplotlib>=3.5.0
seaborn>=0.11.0

# NLP preprocessing (existing dependencies) 
nltk>=3.7

# Optional: GPU acceleration performance boost
# Uncomment if you have CUDA GPU available
# torch-audio  # For better GPU utilization
# pytorch-cuda>=11.7  # For CUDA 11.7+ support
