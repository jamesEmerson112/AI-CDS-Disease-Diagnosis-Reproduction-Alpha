AI-CDS DISEASE DIAGNOSIS - RANKED TODO
Generated 2026-08-05 from docs/plans/. Plain-text digest; the .md files remain authoritative.
Updated 2026-08-06 (late): P4 AND P5 BOTH COMPLETE. Both metric knobs are gone.
Updated 2026-08-10: four phase decisions recorded (below); audit corrections folded in.
Updated 2026-08-11: C1-C9 landed. Phase 2 is DONE and Phase 3's drift-stoppers are
  done in this repo. P14 P15 P16 P17 P20 closed, P18 retired.
Updated 2026-08-11 (later): the whole batch PUSHED (e6bb8e3..a53c84b); C9's SHA
  resolved to 760ff1d; tail commit a53c84b untracked the stale design snapshot
  whose ignore pattern never matched. Next up, per decision 4: P40 code, then
  the ONE combined pod session (P29 staged runs + P40-instrumented run + the
  C4/C5 legacy-baseline byte-compare against results/baseline/05082026_18-55-32).
Updated 2026-08-11 (post-audit batch): D1-D4 landed and the local track is DONE.
  86fb118 the audit itself; 8965546 synthetic sample IDs + P41; 9168389 the
  runtime-count fix reaching the five deferred docs; efa3794 P40's code
  (RankCases.txt + self-selection consumer); 7e49212 P27 as corrected2
  (1716/561/0 measured). Shared golden gate at 7e49212: byte-exact, 42:28 --
  a SEVENTH measurement, which moved the range floor to 42. Suite 413 -> 459
  passed, 3 deselected. What remains is the pod session (see NEXT).
Updated 2026-08-11 (audit): a full pass over all 1463 lines against the tree at
  fcc85a8. This file had ACCRETED rather than been updated: five items sat in
  contradictory checkbox states (P34 five ways; P4, P9, P31, P36 three or two
  ways; P29 in three at once), five items discovered after Tier 1 were missing
  from the ranked checklist entirely (P32 P33 P34 P39 P40 -- including P40, the
  declared highest-value open item), and the "read before designing any
  exact-match metric" section still recommended, as preference #1, a design this
  project DESIGNED AND REJECTED ON MEASUREMENT. Fixed in place. THE INVARIANT
  THIS PASS ESTABLISHES, and the one to preserve: every P-number carries EXACTLY
  ONE checkbox state across the whole file. An item that is genuinely two things
  (P29) is split into two separately-named sub-items rather than left ambiguous.
  Also in this pass: P6 RETIRED, P24 RETIRED, P10/P11/P12 DEMOTED to a new
  optional section, P41 recorded as new.
  THE DISCIPLINE OF THIS FILE IS CORRECTION IN PLACE, NOT PRUNING. Closed items
  keep the evidence that closed them, wrong claims are replaced by the true fact
  with a dated note saying what was wrong, and decision records stay VERBATIM
  even where the outcome differed -- a decision is a record of what was decided,
  not a prediction to be scored.

DECISIONS 2026-08-10 -- settled by the owner, do not re-litigate
  1. PHASE 3 SPLITS. The two drift-stoppers land IN THIS REPO: the single
     PerformanceIndex parser (drift is active -- 4 parsers and ~7 discovery
     rules as of the 2026-08-08 audit, and P5's own analyze_rank_metrics.py
     added the 7th) and the --out/results root (stops hand-moving DUA-covered
     output). The polish -- main.py CLI, SentenceEncoder registry, dashboard --
     moves to the P38 clean repo. Phase 3 "ship point" is redefined
     accordingly: this repo ships when the drift-stoppers land.
  2. bert_eval.py IS DELETED OUTRIGHT, no salvage. The audit proved the
     hf_automodel salvage target never existed in any branch; the GPU-aware
     line is worthless (finding 08); mean-pooling can be rebuilt via the P38
     encoder registry if ever wanted. Same commit must delete its stop_words
     monkeypatch (bert_eval.py:63) and re-count the threshold-literal sites.
     This RETIRES P18.
  3. BASELINE RESIDUAL DEFECTS: fix locally now, verify on the next pod
     session. The baseline has NO golden, so verification is one legacy
     baseline run on Linux byte-compared against the existing 2026-08-05
     artifact in results/. Piggybacks on the pod session that P29/P40 need
     anyway -- no dedicated pod time.
  4. NEXT SESSION IS P40 CODE, then ONE combined pod session runs P29's
     staged attribution runs AND the P40-instrumented run together (plus the
     decision-3 verification run). Most value per pod-hour.

THE REFACTOR BATCH C1-C9, landed 2026-08-10/11, PUSHED to origin/main 2026-08-11
  (e6bb8e3..a53c84b -- the eight code commits, the docs pass, the decisions
  record a725aac, and one tail commit; nothing of the batch remains local)
  Eight code commits plus this documentation pass. The 2026-08-08 audit listed
  every outstanding Phase 2 item and both regressed Phase 3 items; this batch
  closed all of them. Fast suite 264 -> 413 passed, 3 deselected.

    7400eff  C1  all 11 stop_words monkeypatches deleted (P16). The count was
                 11, not the 7 or 8 earlier files claimed -- four sat in
                 non-test code nobody had counted. All were the identical
                 set(stopwords.words('english')), so removal was provably
                 inert. GOLDEN GATE #1: byte-exact, 52:35, on its own commit
                 with nothing else in it.
    5ca7f64  C2  bert_eval.py + the whole evaluation/ package DELETED OUTRIGHT,
                 no salvage (decision 2). RETIRES P18.
    94b4e24  C3  anchored .gitignore Prediction_Output patterns; verified dead
                 code deleted -- scripts/run_all_bert_models.py, entity
                 Admission/Symptom/Drgcodes, print_log,
                 get_diagnosis_similarity_by_description_max_model. The two
                 encoder-independent graders were KEPT, as P4 required.
    9c9e251  C4  baseline run_analysis(encoder=None, config=LEGACY, out=None) +
                 __main__ guard; the two PerformanceIndex handles collapsed to
                 one with-block; run_baseline.py gains full argparse including
                 --pipeline. Closes P17. *** VERIFIED 2026-08-11 *** -- the pod
                 byte-compare against results/baseline/05082026_18-55-32
                 PASSED, 353,418 bytes identical to the trailer cut.
    9d08c94  C5  aicds/runs.py writer side; --out on both arms and both run
                 scripts; check_out_root DUA guard; baseline directory spelling
                 unified to Prediction_Output_BioSentVec_{stamp}. Closes the
                 writer half of P15. GOLDEN GATE #2 at this SHA: byte-exact,
                 ~50 min, covering C2-C5 together.
    1f69e11  C6  aicds/analysis/performance_index.py -- ONE parser, proved
                 equivalent to all four private ones before any deletion.
    7927a88  C7  runs.discover: ONE rule + a Run dataclass. Five consumers
                 rewired, the four private parsers deleted, the equivalence
                 test converted to pinned literals in
                 tests/test_performance_index.py. Closes P20 and the reader
                 half of P15.
    5a52d26  C8  run_metadata.json on both arms, written after the handle
                 closes; config.name_of; fold_dir digest allowlisted to the
                 TRAIN/TEST filenames. Closes P14.
    760ff1d  C9  documentation made true again: CLAUDE.md, this file,
                 revival-roadmap.md, findings/10, architecture.md, setup.md,
                 README.md, pyproject.toml's golden-runtime comment. (The
                 commit wrote "(this)" here because it could not know its own
                 SHA; resolved after the fact.)
    a53c84b  --  tail commit, found during C9's final sweep: the tracked
                 .system-design-visualization.md declared itself
                 local-review-only and claimed .gitignore covered it, but the
                 ignore pattern lacked the .md extension and never matched, so
                 the Feb-2026 snapshot (pre-src/aicds layout, bert_eval as a
                 live component) had been committed anyway. Pattern fixed,
                 file untracked; the local copy stays on disk as its header
                 intended. Lesson filed with the other two ignore-pattern
                 bites: a pattern nobody tests is a pattern that does not
                 match.

  C7 GATE RESULTS, recorded because they are the evidence the rewiring is inert:
    - compare_models: 16 sanity checks pass on results/, results_corrected/ and
      results_drg/ bare; results_p5 needs --pipeline drg and REFUSES bare BY
      DESIGN (it is not in _PIPELINE_BY_DIRNAME). That refusal is the feature.
    - build_readme_plots now RUNS -- it used to raise FileNotFoundError on every
      invocation -- and regenerated all six committed SVGs BYTE-IDENTICAL.
    - build_dashboard_data JSON byte-identical (sha 0638b6c5...).
    - verify_setup exits 0. The output/ check is gone, so any doc claiming it
      "reports one spurious output/ failure" is now false.
    - analyze_performance auto-detect no longer crashes from the repo root
      (bounded to depth=1) but no longer auto-detects a nested results*/ tree
      either. Name such a tree explicitly. That cost was accepted knowingly.

  C8 RESIDUE, interim and by design: every compare_models invocation on the
  three EXISTING trees prints one
      [WARN] no run_metadata.json under <dir>; inferring pipeline from ... (pre-C8 runs)
  line. Nothing retrofits metadata onto a run nobody can re-derive -- the honest
  metadata for such a run is no metadata -- so the noise persists until P29
  produces the first metadata-bearing trees.

  GOLDEN RUNTIME, MEASURED SEVEN TIMES: 42:28, 43:28, 43:50, 44:32, ~50:00, 52:35, 53:10.
  (CORRECTED 2026-08-11 (audit): this canonical line said FIVE and omitted 43:50,
  which P1's own evidence block below has recorded since 2026-08-05. The
  "five times" phrasing had been copied to TWO other places in this file -- P1's
  seam note and P31's Tier 3 checklist entry -- not one; the first pass fixed
  only the seam note and the checklist entry was caught on re-grep. All three
  now say six. The RANGE was never wrong, only the count.)
  Call it 42-53 minutes. Anything claiming ~20 or ~26 minutes is stale.
  tests/test_golden.py:68 STILL SAYS "about 20 minutes" AND WAS LEFT ALONE ON
  PURPOSE: test_golden.py is the binding constraint on this whole refactor, and
  editing the file that holds the safety net to fix a docstring is not a trade
  worth making. Every other statement of the RANGE is now correct, and as of
  the commit after the audit the COUNT is six everywhere too -- CLAUDE.md,
  README.md, setup.md, revival-roadmap.md and pyproject.toml each got the
  one-line fix the prose-scoped audit had to defer. The docstring is the only
  residue of P31 -- see its block.

STATUS
  DONE     P1 P2 P3 P4 P5 P7 P9 P14 P15 P16 P17 P20 P28 P30 P33 P34 P36
           THE FLAT 14-SHA PARENTHETICAL THAT USED TO SIT HERE WAS DROPPED
           2026-08-11 (audit). It did not map onto the items it followed:
           b53024e appeared in it while being attributed to nothing, 31bea66 is
           a plumbing commit rather than a completion, P9 has NO commit at all
           (it is satisfied by P1's seam), and P15 and P20 each consumed two
           SHAs. A list of SHAs that cannot be checked against a list of items
           is a list that will drift, and it had. Per-item attribution now lives
           in each item's own detail block, and the refactor SHAs live in the
           C1-C9 batch table above -- both of which say which commit closed
           which item, which is the thing the parenthetical was trying to say.
           P29 IS NOT DONE and was removed from this line 2026-08-11. It had
               been listed here while also sitting under PARTIAL below and
               unchecked at the ranked-checklist entry, three states at once.
               Its config code landed (45f36fc) and its own detail block says
               so; the RUNS are what is open. SPLIT 2026-08-11 (audit) into
               P29-configs [x] and P29-runs [ ]; see PARTIAL.
           P17 is DONE, baseline half VERIFIED 2026-08-11: the pod's
               --pipeline legacy run byte-matched the 2026-08-05 reference
               (353,418 bytes to the trailer cut). The code landed in 9c9e251;
               the baseline arm has no golden, so that one Linux run
               byte-compared against
               results/baseline/05082026_18-55-32. Until that runs, "DONE"
               here means "written and reviewed", not "proved inert".
           P33 CLOSED 2026-08-11 (audit) by C7's discover(): the run choice is
               now newest-MTIME rather than lexically-last-stamp -- the
               DDMMYYYY string-sort bug this item found is documented at
               runs.py:497-498 -- the pipeline is recorded in run_metadata.json
               and read by compare_models.resolve_pipeline, a mixed-pipeline
               tree is REFUSED rather than silently merged, and the pipeline is
               printed on the PDF cover page. All three consequences the item
               listed are closed.
           P18 RETIRED 2026-08-10 -- not done, deliberately abandoned. The
               salvage target never existed in any branch.
           P24 RETIRED 2026-08-11 (audit) -- superseded by P40, which decided
               the question P24 posed ("implement or delete") in favour of a
               NEW SIBLING FILE and explicitly declines to repair the dead
               handles. See the RETIRED section.
           P4  COMPLETE 2026-08-06. Grader landed (75b6530) and all four arms
               RAN under --pipeline drg. Both predictions written in advance
               HELD; the unpredicted result is that drg-exact reproduces
               corrected cosine at threshold 1.0 BIT-EXACTLY, 144 numbers.
               So P4 did not move the numbers -- it removed the reason to
               doubt them. See docs/findings/12-drg-grader.md.
           P5  COMPLETE 2026-08-06. Code (47e81bd, 5393cab) + four-arm run.
               Golden BYTE-EXACT in 43:28; PerformanceIndex.txt identical to
               the pre-P5 drg run on all four arms; and the 16 compare_models
               sanity checks MINTED FROM THE PRE-P5 RUN pass on the post-P5
               run. Three independent confirmations that P5 is additive.
               RESULT: no pair separates on any population, max |t| = 1.718
               vs 2.262 needed -- the written-in-advance prediction held.
               UNPREDICTED: abstention treatment is a THIRD knob and it
               inverts every sign. See docs/findings/13-rank-aware-metrics.md.
  PARTIAL  P8   80 of 89 fragments; see P27
           P29 IS TWO ITEMS AND THE AUDIT SPLIT IT 2026-08-11, because one
                P-number in two states is exactly the defect this pass exists to
                remove. From here on the two halves are named separately and
                each carries one state:
                  P29-configs  [x] DONE 2026-08-05 (45f36fc) -- the two
                                   PipelineConfigs exist and are selectable.
                  P29-runs     [ ] OPEN -- the eight runs that produce the
                                   attribution. Nothing about them has run.
           P29-runs  deferred 2026-08-06; scope is EIGHT RUNS, 4 arms x 2
                configs (~95 min, ~$1.80). CORRECTED 2026-08-11 (audit): the
                "TO DO" line at the foot of P29's detail block said "run both on
                the baseline arm", i.e. two runs. That was never the design --
                attributing the drop needs every arm under both configs, and the
                staged script already ran all eight. Eight is the number.
                RETIRED 2026-08-11 (audit), per owner decision 3: the STAGED
                /workspace/repo/attribution_runs.sh IS NO LONGER WHAT RUNS. The
                pod gets `git pull` to current main plus a fresh pre-flight
                (minutes), and the runs use the simplified two-invocation
                --pipeline X --out results_X form below. Reason it had to be
                retired rather than kept: the script was pre-flighted at repo
                5fbdb2f, which predates C5/C7/C8, so it writes the OLD flat
                cwd layout with the baseline's space spelling -- output that
                post-C7 runs.discover REFUSES -- and it predates
                run_metadata.json, so its trees would carry no provenance at
                all. Dirname attribution, in the one experiment whose entire
                purpose is attribution. The pre-flight record below is kept as
                HISTORY: it is the evidence the pod can do the work, and the
                environment facts in it (vCPU, memory headroom, import order)
                are still true.
                --- pre-flight record, 2026-08-06, kept as history ---
                /workspace/repo/attribution_runs.sh on the pod was written,
                pre-flighted and unlaunched. Resume was ONE command:
                  cd /workspace/repo && setsid nohup ./attribution_runs.sh \
                      > attribution_driver.log 2>&1 < /dev/null &
                ~95 min for all 8 runs (4 arms x 2 configs), ~$1.80.
                Pre-flight PASSED 2026-08-06: both configs resolve
                (folds-only = folds_grouped+legacy, preprocess-only =
                folds+corrected, grader cosine for both), nltk+torch+sent2vec
                all import, both fold dirs present at 10 each, 32 vCPU,
                128 GB cgroup vs ~22 GB peak. Repo at 5fbdb2f. The drg-run
                output was moved to _done_drg/ so the harvest globs cannot
                pick it up -- it is already downloaded as results_p5/ locally.

                THE SCRIPT HARVESTS PER BATCH, NOT AT THE END, and that WAS
                load-bearing when it was written: P14 was unimplemented, so
                NOTHING in a run's output recorded which pipeline produced it.
                Both batches emit identically-named
                Prediction_Output_<Model>_<timestamp> dirs into one cwd, so
                sorting them afterwards means inferring the config from
                timestamp order alone -- in the one experiment whose entire
                purpose is attribution. Mis-filing a folds-only number as
                preprocess-only would invert the conclusion.

                UPDATE 2026-08-11: C5 and C8 removed the hazard. --out writes
                ROOT/{key}/{stamp}/ directly, so the two batches cannot collide
                in one cwd, and run_metadata.json records the pipeline by name
                AND by all three PipelineConfig fields. The simplified form is
                two invocations per arm with NO harvest step at all:

                  python scripts/run_bert_analysis.py --model all \
                      --pipeline folds-only      --out results_folds_only
                  python scripts/run_bert_analysis.py --model all \
                      --pipeline preprocess-only --out results_preprocess_only
                  python scripts/run_baseline.py \
                      --pipeline folds-only      --out results_folds_only
                  python scripts/run_baseline.py \
                      --pipeline preprocess-only --out results_preprocess_only

                *** SUPERSEDED 2026-08-11 (audit) -- THIS IS NOW WHAT RUNS. ***
                This paragraph used to say the opposite: "THAT IS THE FUTURE
                FORM, NOT WHAT RUNS NEXT", on the reasoning that rewriting a
                pre-flighted script invalidates the pre-flight, and that the
                pod session was already carrying enough. That reasoning was
                sound when written and is now WRONG, because the thing it was
                protecting no longer exists: the pre-flight was taken at repo
                5fbdb2f, and main has since moved through C5, C7 and C8. A
                pre-flight is only worth something if the tree it was taken on
                is the tree that runs. Owner decision 3 (2026-08-11): pull the
                pod to current main, re-pre-flight -- it costs minutes, and it
                is the same check, taken on the tree that will actually
                execute -- and run the eight invocations above. The reward for
                the pull is not tidiness: --out removes the collision that
                forced the per-batch harvest, and run_metadata.json makes these
                THE FIRST METADATA-BEARING TREES in the project, which is also
                what silences the C8 [WARN].

                Note also: the two new roots must be gitignored before anything
                is written into them. results_folds_only/ and
                results_preprocess_only/ both match .gitignore's results*/ glob,
                which is precisely why that line is a glob and not a hand-kept
                list. check_out_root (C5) verifies it by asking git, and refuses
                the run otherwise.
           P32  all four arms RAN; root README REWRITTEN (e83b5be) and again
                for P4+P5; comparison PDF BUILDS for all pipelines (P34 fixed);
                docs/README.md refreshed (2e5c6f3)
  NEXT     THE POD SESSION (D5) -- everything local is done as of 2026-08-11:
                P40's code landed (efa3794, RankCases.txt + the self-selection
                consumer), P27 landed as corrected2 (7e49212), and the shared
                golden gate at 7e49212 was byte-exact in 42:28. The pod session
                runs: the C4/C5 legacy-baseline byte-compare, P29's eight
                attribution runs, and the 4-arm drg run that produces the first
                RankCases-bearing trees and executes the self-selection test.
                Optional riders: a corrected2 4-arm run, the P13 real-model
                baseline golden mint.
           P38 (clean public repo) is the hard BLOCKER on publication
  NEW      P27 P29 P30 P31 P32 P33 - found while implementing Tier 1
           P34 P35 P36 P37 - found while implementing P4. (P34 MOVED HERE
                2026-08-11 (audit) from the Tier 1 line above: its own detail
                block dates it 2026-08-06 and attributes it to the corrected
                comparison PDF, which is P4-era work, not the 2026-08-05
                Tier 1 window.)
           P39 P40 - found while implementing P5 (tie-breaks; per-case output)
           P41 - found by the C1-C9 audit (the -m env hole, fixed 8965546)
           P42 - found by D5's first-ever Linux run of the fast suite
                 (fold-split environment dependence; finding 14)
           P41 - found 2026-08-11 by the C1-C9 audit (the `-m` entry point
                silently ignores AICDS_PIPELINE on the BERT arm only)

THE RESULT SO FAR - all four arms, corrected pipeline, 2026-08-06
Full writeup in docs/findings/11-corrected-pipeline-first-results.md.

  MAX @ threshold 1.0 -- the ONLY setting where no model saturates
                       legacy   corrected    delta
    BioSentVec (700D)  0.1843     0.0877    -0.0966
    Bio_ClinicalBERT   0.1462     0.0862    -0.0600
    BiomedBERT         0.1692     0.0855    -0.0837
    BlueBERT           0.1776     0.0785    -0.0991
    SPREAD             0.0381     0.0092

  CORRECTED 2026-08-06: the spread collapse above is MAX-ONLY. Do not
  generalise it - that error was committed once already, in finding 11's
  original headline, and had to be walked back. Under every TOP-K the spread
  WIDENS:

    aggregator @ 1.0    legacy   corrected   direction
    MAX                 0.0381     0.0092    down 4.1x
    TOP-10              0.0462     0.0671    UP 1.45x
    TOP-20              0.0506     0.0819    UP 1.62x
    TOP-30              0.0744     0.1383    UP 1.86x

  Normalising by the leading value does not rescue it (MAX 20.7 -> 10.5%,
  TOP-10 16.2 -> 26.9%). So "the encoders converged" is a statement about
  choosing MAX, not a finding.

  WHAT REPLACES IT - the ordering inverts on an arbitrary knob. Corrected,
  threshold 1.0 held fixed, changing only K:

                        MAX      TOP-10   TOP-20   TOP-30
    BioSentVec        0.0877/1  0.2163/2 0.2229/4 0.2296/4
    Bio_ClinicalBERT  0.0862/2  0.2491/1 0.3049/1 0.3513/2
    BiomedBERT        0.0855/3  0.1981/3 0.2888/3 0.3353/3
    BlueBERT          0.0785/4  0.1821/4 0.3038/2 0.3679/1

  BioSentVec 1st -> 4th; BlueBERT 4th -> 1st, same data, same threshold. The
  MECHANISM: the baseline abstains on 24.4% of cases (PR 0.7558) while every
  BERT arm predicts on 100%, so widening K cannot help the baseline where it
  declined to predict but gives each BERT arm another free guess, and one hit
  inside K suffices with no penalty for the other K-1. TOP-K STRUCTURALLY
  REWARDS NOT ABSTAINING. The defensible claim is that the ranking is decided
  by two arbitrary knobs - aggregator and threshold - so NO ENCODER RANKING IS
  SUPPORTED. Finding 05 still predicted the drop correctly; what it did not
  predict is that the residual gap depends on the aggregator.

  Baseline TOP-10 @ 0.6:  0.4824 -> 0.3922  (published 0.489)
  i.e. roughly a fifth of the headline number was leakage + preprocessing.
  The three BERT arms cannot move there; they are pinned at 1.000.

  SATURATION AND DEGENERACY BOTH SURVIVE. All three BERT arms still report
  PR exactly 1.0000 with P == R == F in every row, so every "F1" in those
  columns is still ACCURACY. The baseline still abstains at PR 0.7558 and
  alone has P != R - which confirms degeneracy is a consequence of BERT's
  compact embedding space, not a structural property of the code.

  Tier 1 removed two of the four documented confounds: PATIENT LEAKAGE (41 leaked
  test cases -> 0) and DIVERGENT CROSS-ARM PREPROCESSING (26/145 matching -> 145/145).
  SUPERSEDED 2026-08-06: the other two are now DONE as well. SELF-GRADING (P4) is
  closed by the drg-exact grader, and RANK-BLINDNESS (P5) by MRR. All four are in.

THE KNOBLESS RESULT - P5, all four arms, --pipeline drg, 2026-08-06
Full writeup in docs/findings/13-rank-aware-metrics.md.

  With the threshold knob gone (drg-exact collapses all five rows) and the K
  knob gone (MRR has no K), this is the first number the project has produced
  with ZERO reported knobs. 76 winnable cases, mean-of-per-fold-rates:

                      MRR@50   Hit@1   Hit@50    P@1     P@50   coverage
    Bio_ClinicalBERT  0.2462  0.1749   0.6967  0.1749  0.0290    1.0000
    BiomedBERT        0.2432  0.1693   0.6656  0.1693  0.0280    1.0000
    BlueBERT          0.2314  0.1549   0.7300  0.1549  0.0316    1.0000
    BioSentVec        0.2029  0.1484   0.3577  0.1484  0.0857    0.8477

  NO PAIR SEPARATES. Max |t| over all six pairs: 1.718 (winnable), 1.651
  (all-cases), 1.174 (answered) -- all under the 2.262 needed for p<0.05. The
  written-in-advance prediction held on every population.

  PRECISION@K INVERTS THE TOP-K CURVE, as designed. It FALLS with K for every
  arm (6.0x from K=1 to 50 for Bio_ClinicalBERT) where TOP-K rises for every
  arm, and it is NOT monotone (BioSentVec 0.0868 -> 0.0845 -> 0.0855 ->
  0.0858 -> 0.0857), so unlike Hit@K nothing forces its direction. Within-arm
  only: truncating your own candidate list inflates reported P@K 3.53x.

  THE UNPREDICTED RESULT - A THIRD KNOB WAS UNDERNEATH. MRR is not abstention-
  neutral: a declined case scores RR=0 while an arm that always offers 50
  candidates has some chance of a hit. The baseline abstains; the BERT arms
  never do. So the CONVENTION reorders all four arms:

    abstention scores 0     Bio_ClinicalBERT > BiomedBERT > BlueBERT > BioSentVec
    abstention excluded     BioSentVec > Bio_ClinicalBERT > BiomedBERT > BlueBERT
                            (0.1474)     (0.1321)          (0.1302)     (0.1229)

  EVERY SIGN FLIPS; the baseline goes last to first. Neither convention is
  neutral and THERE IS NO THIRD THAT IS -- penalising abstention charges "I
  don't know" the same as a wrong answer, while excluding it scores the
  baseline on 98 self-chosen cases against the BERT arms' 129. Abstention is a
  property of the ARM, not of the metric, so this knob cannot be designed away.
  All three populations are reported; the conclusion survives all three.
  Quantifying the self-selection half of it is P40.

  THE VALIDATION THAT MAKES IT TRUSTWORTHY. RankMetrics.txt's three populations
  reproduce legacy's P / R / PR columns EXACTLY, from an independent code path:
    R  0.19230769230769232 == all-cases Hit@10
    P  0.25124098124098121 == answered  Hit@10
    PR 0.75576923076923075 == coverage
  Exact to the last bit, and predicted in rank_metrics.py's N2 note before the
  code existed. This also RETIRES the degeneracy confusion for good: P == R was
  never the metric collapsing, it is PR == 1.0, i.e. the answered and all-cases
  populations being the SAME SET because those arms never abstain. The columns
  were never wrong, only unlabelled.

Three plan files, three different jobs:
  correctness-fixes.md  what is scientifically wrong, and in what order to fix it
  metric-redesign.md    what to replace the metric with
  revival-roadmap.md    the refactor sequence (Phases 0-5) and current status

RANKED FOR ONE GOAL: producing a DEFENSIBLE BioSentVec-vs-3-BERT comparison.
Ranked differently if the goal were "ship a clean package" - the refactor items would
rise. They are ranked low here on purpose, not because they are worthless.

THE RULE THAT SHAPES EVERYTHING
  Refactor work must NOT move the numbers; `pytest -m golden` compares byte-for-byte.
  Correctness work DELIBERATELY moves the numbers.
  They cannot run at the same time. Choosing the science first means FREEZING the
  refactor at its current point, which is a legitimate call - just make it knowingly.


===============================================================================
RANKED MASTER LIST
===============================================================================

TIER 1 - BLOCKS ANY VALID COMPARISON. Nothing downstream means anything until these land.

  [x] P1   Decide the re-baselining strategy      DONE 2026-08-05 (c2115ba)
           Chose ADDITIVE. src/aicds/config.py holds PipelineConfig(fold_dir,
           preprocess_version) with LEGACY and CORRECTED. LEGACY is the default
           everywhere and stays bit-identical forever, so the golden is NEVER
           re-minted. Verified: pytest -m golden passed TWICE, 43:50 on the
           default seed and 44:32 under PYTHONHASHSEED=12345.
  [x] P2   A1. GroupKFold on SUBJECT_ID           DONE 2026-08-05 (c2115ba)
           scripts/make_folds.py -> data/folds_grouped/. Leakage 41/129 -> ZERO,
           recounted independently rather than trusting the script's --verify.
           Folds are now uneven (114/15 .. 117/12) because subject 41976's 15
           admissions must land together. Output is GITIGNORED, not committed:
           deterministic regeneration beats adding 1290 more lines of
           DUA-covered text to a public repo.
  [x] P3   A4c. Unify cross-arm diagnosis preprocessing  DONE 2026-08-05 (c2115ba)
           One line. embending_diagnosis embeds the PREPROCESSED text but keys
           the dict by the RAW description, so the BERT arm's keys were already
           right -- only the encode input was wrong.
  [x] P4   A2. Encoder-independent grader        DONE 2026-08-06 (75b6530 + run)
           drg-exact reproduces corrected cosine @1.0 BIT-EXACTLY on all four
           arms (144 numbers). So it did not move the numbers -- it removed the
           reason to doubt them. finding 12.
  [x] P5   B1. Rank-aware metrics (Option C)     DONE 2026-08-06 (47e81bd,
           5393cab + run). No pair separates on any population, max |t| 1.718.
           Additive, confirmed three ways. Found a THIRD knob: abstention
           treatment inverts every sign. finding 13.

TIER 2 - MAKES THE REPORTED NUMBERS HONEST. Each is independently worth doing.
  RESTRUCTURED 2026-08-11 (audit). The open half of this tier is now exactly
  three items, in order. P6 was RETIRED, P10/P11/P12 were DEMOTED off the
  publication path (see DIMINISHED PAYOFF below), and P29 appears here as
  P29-runs only, because its configs are done. The closed items stay as history.

  [~] P40  Per-case output (new sibling file)     CODE DONE 2026-08-11 (efa3794)
           RankCases.txt lands as the third after-the-close sibling; the
           self-selection consumer landed in the same commit and degrades with
           a [WARN] on pre-P40 runs. What remains is the RUN: the pod's 4-arm
           `--pipeline drg --out results_p40` pass produces the first
           RankCases-bearing trees, and the baseline's file is the load-bearing
           one (the restriction needs both sides). Golden gate at 7e49212:
           byte-exact in 42:28. Detail section at the foot of this file.
  [x] P27  Finish the comma rejoin - 9 residual   DONE 2026-08-11 (7e49212)
           Landed as the THIRD preprocess_version `corrected2` (owner decision
           4): `corrected` stays FROZEN, so every existing results tree, every
           finding-11 and finding-13 number, and the pod byte-compare stay
           reproducible. Measured on the real data: 1716 tokens, 561 unique,
           ZERO residual fragments, firing exactly 9 times across exactly the
           4 labels. The rule that shipped is lowercase tail + UPPERCASE
           accumulated predecessor (the ICD-9 short-title tell) + the 24-char
           cap; the dataset pins and the predicate pins are separate on
           purpose -- the counts were measured BLIND to a widening (caps 25-40
           leave them unmoved), so the cap constant and a 25-char boundary
           fixture pin the predicate itself. Fresh corrected2 NUMBERS need a
           pod run (optional D5 step; nothing existing moved).
  [ ] P29-runs  The eight attribution runs        (pod, ~95 min)
           4 arms x 2 configs (folds-only, preprocess-only), simplified
           --pipeline X --out results_X form, on a pod pulled to current main.
           P29-configs is [x] DONE (45f36fc) and is NOT this item.

  --- closed / partial, kept as history ---
  [x] P7   A4a. Fix "w/o" -> "w" negation loss    DONE 2026-08-05 (c2115ba)
           Under CORRECTED, "w/o" now survives as "without". LEGACY still
           collapses it to "w", as pinned.
  [~] P8   A4b. Fix comma-split fragments         PARTIAL 2026-08-05 (c2115ba)
           Recovers 80 of 89 (1805->1725 tokens, 573->564 unique). NINE
           OCCURRENCES REMAIN -- see P27. (CORRECTED 2026-08-11 (audit): this
           line used to call P27 "the highest-value unfinished item in this
           file". P40 is; P27 is second. The STATUS block has said so since
           2026-08-06 and this line was never updated to match.)
  [x] P9   B5. Keep the legacy number, labelled   SATISFIED BY P1's SEAM
           LEGACY is a permanent, selectable, byte-reproducible path rather than
           a number copied into a report. Nothing further needed here.
  [~] P32  Collect the corrected-pipeline results PARTIAL 2026-08-06 (3b8aed9)
           All four arms ran and are written up in finding 11. Two things
           remain and both are runs, not code: P29-runs, and the
           analyze_score_distributions re-run (P37). Added to this checklist
           2026-08-11 (audit) -- it had a detail block but no checklist entry.

TIER 3 - PROTECTS THE WORK. Cheap insurance; do P13/P14 before touching shared code.

  [x] P28  Guard make_folds.py --out              DONE 2026-08-05 (930c162)
  [x] P30  Widen the pre-commit hook to cover     DONE 2026-08-05 (d77de02)
           data/folds_grouped/ and any future fold set
  [x] P33  compare_models silently picks one run  DONE 2026-08-11 (7927a88)
           runs.discover picks by newest MTIME, prints the choice when a key
           has more than one run, records the pipeline via run_metadata.json,
           refuses mixed-pipeline trees, and puts the pipeline on the PDF
           cover. Added to this checklist 2026-08-11 (audit).
  [x] P34  compare_models sanity checks keyed     DONE 2026-08-06 (b53024e)
           by pipeline. SANITY_CHECKS_BY_PIPELINE + HOSTS_BY_DATE. Added to
           this checklist 2026-08-11 (audit).
  [ ] P39  The two arms break score ties          (1 h + one golden run)
           differently. Bounded at <= 0.0022 MRR; needs its own `ranking`
           config field and its own golden run, so it is not free. Added to
           this checklist 2026-08-11 (audit).
  [ ] P38  CODE-ONLY PUBLIC REPO, data removed    (days)  <- BLOCKS PUBLICATION
           NOW ALSO CARRIES the Phase 3 polish, per decision 1: P19 CLI,
           P21 encoder registry, P22 dashboard, P23 deprecation shims, the
           F1 -> accuracy rename. (P23 added to this carry list 2026-08-11
           (audit) -- decision 1 moved it with the rest of the polish but this
           line never listed it. It is NOT cosmetic: scripts/ now holds the
           check_out_root DUA guard and the config banners, so shimming
           scripts/ means RELOCATING A SAFETY MECHANISM, not forwarding a
           call.)
  [ ] P13  C11. Mint a baseline-arm golden        (13 min compute, Linux only)
  [x] P14  C10. Result provenance (run_metadata.json)  DONE 2026-08-11 (5a52d26)
           Both arms write it LAST, after the PerformanceIndex handle closes.
           Records git SHA + dirty, the pipeline by NAME and by all three
           PipelineConfig fields, a SHA-256 of the fold split's TRAIN/TEST
           contents, model key + label, K_FOLD, platform/python/numpy.
           write_run_metadata NEVER RAISES -- a finished 13-minute run must not
           be lost to a git subprocess call about provenance.
           TWO DESIGN NOTES WORTH KEEPING. (1) The digest is ALLOWLISTED to
           Constants.TRAIN/TEST rather than hashing every file under the
           directory: the owner's Windows checkout carries a hidden 105-byte
           desktop.ini in every data/folds/Fold*/ that git has never tracked,
           so the byte-identical legacy split hashed to b7554079... there and
           93f0943a... on the Linux pod -- in the one field whose entire job is
           saying whether two runs saw the same data. Explorer also rewrites
           desktop.ini when a folder view changes, so an untouched split would
           start reporting itself as a different one. Local now matches the
           predicted Linux 93f0943a... (2) run_metadata.json is deliberately
           NOT the run marker; PerformanceIndex.txt keeps that job, so every
           pre-C8 run stays discoverable.
  [x] P15  C2.  Unify output paths + the five globs   DONE 2026-08-11
           Writer half 9d08c94, reader half 7927a88. Both arms emit
           Prediction_Output_{Name}_{stamp} via aicds.runs.run_dirs; FIVE
           consumers call runs.discover, ONE rule -- analyze_performance.py:71,
           analyze_rank_metrics.py:149, build_dashboard_data.py:156,
           build_readme_plots.py:302, compare_models.py:282/:355. That is
           THREE of finding 10's five tabulated sites plus the two later rules,
           NOT all five of the table: the table's other two entries are the
           deliberate exceptions below. Two DELIBERATE exceptions
           remain and are not drift: tests/test_golden.py keeps its own tmp-dir
           glob and its \d{8}_\d{2}-\d{2}-\d{2} regex -- that regex is what
           FORCED the underscore direction, and the golden must not depend on
           the code it audits -- and test_reorganization.py:75 keeps a
           recursive ** because it asserts that committed results exist at all
           rather than discovering a run.
  [~] P31  Correct the golden's stated runtime    MOSTLY DONE 2026-08-11 (C9)
           Every doc now says 42-53 min; the golden was measured SEVEN times (42:28 arrived with the D3+D4 gate), not
           the five this line claimed until 2026-08-11 (audit) -- it was the
           third count-bearing site in this file and the one the first pass
           missed. Only tests/test_golden.py:68 still says "about 20 minutes",
           and it was left alone on purpose -- see the runtime note in the
           STATUS block.

TIER 4 - FINISH THE REFACTOR. The drift-stoppers landed here; polish moved to P38.

  [x] P16  C1.  Delete the stop_words monkeypatches   DONE 2026-08-11 (7400eff)
           THE COUNT WAS 11, not the 8 this line used to claim and not the 4
           the roadmap claimed. All were the identical
           set(stopwords.words('english')), so removal was provably inert;
           golden byte-exact afterwards at 52:35, on a commit containing
           nothing else.
  [x] P17  C4.  Dead code + the last 2 baseline defects  DONE 2026-08-11
           Dead code 94b4e24; baseline 9c9e251, VERIFIED 2026-08-11 by the
           pod byte-compare (353,418 bytes identical). See the P17 detail
           section for what the old description of the two handles got WRONG.
      P18  C3.  Salvage hf_automodel before deleting bert_eval.py
           RETIRED. Its checkbox moved to the RETIRED section below 2026-08-11
           (audit) so that every retired item sits in one place; the slot is
           kept here because C3 is part of the refactor sequence and a gap in
           it reads like an omission.
  [ ] P19  C6.  A real CLI (no main.py; input() still blocks automation)
           MOVED TO P38 by decision 1.
  [x] P20  C7.  ONE PerformanceIndex parser replacing FOUR  DONE 2026-08-11
           1f69e11 added aicds/analysis/performance_index.py and proved it
           equivalent to all four private parsers on the committed goldens;
           7927a88 deleted them and converted the equivalence test to pinned
           literals in tests/test_performance_index.py. The count in the old
           line ("three") was already stale when it was written.
  [ ] P21  C5.  SentenceEncoder Protocol + encoder registry
           MOVED TO P38 by decision 1.
  [ ] P22  C8.  Fix the dashboard (404s today)
           MOVED TO P38 by decision 1. The builder and the fetcher already
           AGREE on dashboard/public/data/; only the committed JSON sits at the
           stray dashboard/dashboard/ path. Fix = re-run the builder, commit
           the output, delete the stray tree.
  [ ] P23  C9.  scripts/ -> deprecation shims
           MOVED TO P38 by decision 1.

TIER 5 - NICE TO HAVE.

      P24  C12. Per-case output files are all empty (decide: implement or delete)
           RETIRED. Its checkbox moved to the RETIRED section below 2026-08-11
           (audit); P40 decided the question it posed.
  [ ] P25  C13. Fold parallelisation via multiprocessing
  [ ] P26  Retrieval interface -> local decoder (the RAG endgame)

DIMINISHED PAYOFF - OPTIONAL, OFF THE PUBLICATION PATH.
  Added 2026-08-11 (audit), per owner decision 2. These three were written as
  Tier 2 items in 2026-08-05, BEFORE P4 and P5 landed. None of them is wrong and
  none is retired -- but the metric each was designed to rescue is no longer the
  metric the project leads with, so each now buys strictly less than its own
  description implies. Ordered by what is left of the payoff.

  [ ] P10  A5. Centre the embeddings              (NOT one line, see below)
           Still the only probe of anisotropy this project has. But what it can
           improve is the COSINE-graded tables only -- the headline path is
           drg-exact, where there is no cosine to un-skew. "ONE LINE" counts the
           subtraction and excludes what it actually costs: a config-seam field
           (numbers move, so it cannot ride an existing pipeline name -- P35's
           rule) plus a golden gate. Line-drift note: the normalize_embeddings
           remark that used to point at bert_models.py:337-343 has TWO sites
           now, :352-358 and :261-267, and it remains a red herring at both --
           L2-normalising before a cosine that divides by the norms is
           algebraically a no-op.
  [ ] P12  B3. MEAN aggregator alongside MAX      (hours)
           Right framing is a REGISTERED AGGREGATOR, not a replacement: MAX
           stays, MEAN sits beside it, and a reader can see both. Two duties
           come with it. (1) SORT BEFORE REDUCING -- a mean over hash-ordered
           labels reintroduces last-ULP nondeterminism, which finding 12 hit on
           HADM 178513 and which no test would have caught. (2) It is a new
           config axis, so SUPPORTED_GRADERS-style refusal plus a banner line
           plus its consumer land in the SAME commit (P35).
  [ ] P11  B4. Per-model threshold calibration    (hours)
           LIKELY MOOT. drg-exact collapses the five threshold rows to one, so
           on the headline path there is no threshold left to calibrate. It is
           also the HIGHEST GOLDEN-RISK surface of the three: the thresholds are
           duplicated across seven set literals whose ITERATION ORDER decides
           output row order, plus a hand-kept matching list at
           bert_models.py:511. And its evidence base is
           analyze_score_distributions.py, which is P37 -- unfixed, and
           measuring with a retired ruler. Fix P37 first or the calibration
           targets are legacy-measured.

RETIRED - DELIBERATELY ABANDONED, NOT DONE. Kept with the reasoning, because a
  silently vanished item reads as an oversight and invites someone to re-open it.

  [-] P6   B2. Set-level soft P/R/F1 (Option B)
           RETIRED 2026-08-11 by owner decision 1. Its stated motivation was
           KILLING THE DEGENERACY -- and findings 04 and 13 dissolved that
           motivation rather than satisfying it: P == R was never the metric
           collapsing, it is PR == 1.0, i.e. the answered and all-cases
           populations being the same set because those arms never abstain. The
           columns were unlabelled, not wrong. Worse, the proposed method is
           cosine over the diagnosis embeddings, which REINTRODUCES THE
           SELF-GRADING CONFOUND P4 removed: the arm that produced the retrieval
           would once again be the arm marking it. Retiring beats implementing
           because implementing would undo a closed fix in order to solve a
           problem that turned out to be a labelling error.
           WHAT SURVIVES, and it is worth keeping: the underlying idea that a
           prediction SET should be scored against a truth SET, so that missing
           a second true diagnosis costs something. Nothing in the project
           measures that today. If it is ever wanted, build it on the
           encoder-independent DRG labels, not on cosine.
  [-] P18  C3. Salvage hf_automodel before deleting bert_eval.py
           RETIRED 2026-08-10 by decision 2, then executed as a plain deletion
           in 5ca7f64. The salvage target NEVER EXISTED in any branch.
  [-] P24  C12. Per-case output files are all empty
           RETIRED 2026-08-11 (audit): SUPERSEDED BY P40, which answers P24's
           own question ("implement or delete") with a third option P24 did not
           consider -- write a NEW SIBLING FILE and leave the dead handles dead.
           That is a decision, not an oversight: repairing the handles would put
           new writes inside the region the byte-exact golden covers, for output
           nothing reads. P40 carries the work; P24 carries nothing.

TIER 3 ADDITIONS from P4 (these protect a result from being silently wrong).

  [ ] P35  No config axis may be unannounced      (minutes) <- NEW, partly done
  [x] P36  bert_models' private containGreaterOrEqualsValue    DONE 2026-08-06 (1d68df9)
  [ ] P37  analyze_score_distributions.py re-implements the grader   (hours)
  [x] P41  `python -m aicds.models.bert_models` ignores AICDS_PIPELINE
           DONE 2026-08-11 (8965546). Found by the audit as a P35-class hole,
           asymmetric between the arms; the -m path now resolves via from_env()
           with the same explanatory comment as the baseline twin.
  [~] P42  The grouped fold split depends on the numpy MAJOR version
           FOUND AND MITIGATED 2026-08-11 (finding 14). GroupKFold breaks its
           ~85 single-admission ties through np.argsort, whose unstable-sort
           behaviour changed between numpy 1.x and 2.x: Windows/numpy-1.26 and
           pod/numpy-2.0 deterministically build DIFFERENT leak-free splits
           from identical inputs. The pod's split is CANONICAL (every
           committed tree used it; the pod regenerates it bit-exactly), pinned
           as CANONICAL_FOLDS_GROUPED_DIGEST in make_folds.py --verify (warns)
           and test_populations.py (fails with an explanation). Finding 12's
           per-fold range corrected in place (3/12 was the Windows split;
           canonical is 4/13 to 13/15); winnable TOTAL 76/129 is
           split-invariant, headline claims untouched. REMAINS [~]: pinning
           numpy==2.0.x in environment.yml is deferred until local
           torch/numpy-2 compatibility is tested -- the digest guards keep the
           divergence loud meanwhile. Caught BY C8's fold_dir digest field
           five days after P14 shipped it: the field's first live use.

WHY THIS ORDER
  P1-P3 and P7 are DONE (2026-08-05, commits c2115ba + 31bea66). P8 is partial.
  SUPERSEDED 2026-08-06: P4 AND P5 ARE BOTH DONE. What follows is the reasoning
    that put them at the top, kept because it explains the ordering, not the state.
  P4 was the top item: with leakage gone and preprocessing unified, SELF-GRADING
    was the largest remaining threat to a cross-encoder claim - all four arms
    used the same embedding space to retrieve AND to judge, so a more compressed
    space graded itself more leniently. Outcome: the bias was POSSIBLE but never
    ACTUAL, which only measuring it could establish.
  P5 followed P4 because it needs P4's encoder-independent relevance label.
  P40 is now the top item, and it inherits P5's open question rather than
    starting a new one - see the P40 detail section.
  P28 before any further fold work - one careless --out overwrites data/folds/ and
    destroys the golden's inputs. Minutes to fix; unbounded to recover from.
  P27 is small, measured, and lands in the same function as P8.
  P29 is trivial and buys real explanatory power: CORRECTED currently bundles the
    fold fix and the preprocessing fix, so their effects cannot be told apart.
  P13 sits above the rest of the refactor because the baseline arm now works and has
    zero regression protection, and cython_utils.py is SHARED between the arms.

  --- why the original order was what it was ---
  P1 first because it was an undocumented decision that P2 forced and nobody had made.
  P2 next because the leakage is ~10x the effect under study, and it affects BOTH arms.
  P3 third because it is cheap and targets the baseline-vs-BERT question directly.


===============================================================================
TIER 1 DETAIL
===============================================================================

[x] P1. DECIDE THE RE-BASELINING STRATEGY          RESOLVED 2026-08-05 (c2115ba)

        DECISION: ADDITIVE, not replacement. Nothing is ever re-minted.

        src/aicds/config.py holds a frozen dataclass:
            PipelineConfig(fold_dir="folds", preprocess_version="legacy")
            LEGACY    = PipelineConfig()
            CORRECTED = PipelineConfig("folds_grouped", "corrected")
        LEGACY is the default at every call site, so tests/golden/stub768/ stays
        valid FOREVER and the published 0.489/0.512/0.521 lineage stays reproducible.
        CORRECTED is the scientifically defensible path. Both live side by side,
        which also satisfies P9 for free.

        Reachable from a shell as of 31bea66 - before that the seam existed but no
        CLI could select it, which made it decorative:
            python scripts/run_bert_analysis.py --model all --pipeline corrected
            AICDS_PIPELINE=corrected python scripts/run_baseline.py
        (HISTORICAL, and no longer true as of C4/9c9e251: the baseline needed the
        env var because baseline_sent2vec.py executed at import time and took no
        arguments. It now has run_analysis(encoder=None, config=LEGACY, out=None)
        at :187 and a __main__ guard at :543, and scripts/run_baseline.py has a
        full argparse with --pipeline and --out; the env var still works via
        from_env() when --pipeline is omitted. See P17.)

        EVIDENCE THE SEAM HOLDS: pytest -m golden passed TWICE after all of
        P2/P3/P7/P8 landed - 43:50 on the default seed, 44:32 under
        PYTHONHASHSEED=12345. Fast suite 122 passed, 2 deselected.

        NOTE FOR ANY DOC EDIT: the golden takes 42-53 MINUTES, measured SEVEN
        times (42:28, 43:28, 43:50, 44:32, ~50:00, 52:35, 53:10). Every doc says the
        RANGE correctly as of C9, and as of the commit after the audit
        (2026-08-11) the COUNT is six everywhere too: CLAUDE.md, README.md,
        docs/guides/setup.md, docs/plans/revival-roadmap.md and pyproject.toml
        were each given the one-line fix this note used to defer. The
        single remaining "~20 minutes" is tests/test_golden.py:68, deliberately
        untouched. See P31. (Count corrected from five to six 2026-08-11
        (audit): 43:50 is recorded four lines above this one, in this very
        block, and was missing from every summary of it.)

[x] P2. A1 - REGROUP THE FOLDS BY PATIENT (GroupKFold on SUBJECT_ID)
        DONE 2026-08-05 (c2115ba). scripts/make_folds.py -> data/folds_grouped/.

        RESULT: 41 leaked test cases -> ZERO. Recounted independently from the
        generated files rather than trusting the script's own --verify:
            leaked test cases (all folds) : 0
            total test cases across folds : 129
            duplicate test HADM_IDs       : 0
        Folds are uneven as predicted (114/15 through 117/12) because subject
        41976's 15 admissions must land in one test set together.

        THE GENERATED FOLDS ARE GITIGNORED (.gitignore:129), NOT COMMITTED.
        GroupKFold(n_splits=10) with the default shuffle=False is deterministic,
        so the folds are a pure function of committed data plus a committed
        script. Regenerate with:  python scripts/make_folds.py
        This keeps 1290 more lines of DUA-covered MIMIC text out of a public repo.

        ALL THREE "STILL OPEN" LINES THAT USED TO SIT HERE ARE CLOSED, and were
        replaced by this note 2026-08-11 (audit). They had been true when
        written and were never revisited:
          - the make_folds.py --out guard is P28, DONE 2026-08-05 (930c162);
          - the .githooks/pre-commit widening is P30, DONE 2026-08-05 (d77de02);
          - THE CEILING WAS RE-MEASURED. 76 of 129 = 58.9% on folds_grouped,
            against 75/129 = 58.1% reproduced on the leaky folds. Both figures
            are PINNED in tests/test_drg_grader.py, so they cannot rot again.
            The leakage fix moved retrievability by exactly ONE CASE, which is
            the interesting part: leakage and label sparsity are independent
            defects, and fixing one told us nothing about the other.

        --- original notes, kept for reference ---
        Why: folds split on HADM_ID, but 129 admissions come from only 100 patients
        (one patient has 15). 41 of 129 test cases (31.8%) have another admission from
        the same SUBJECT_ID sitting in their own retrieval pool. Per-fold contamination
        15%-54%. Measured inflation at threshold 1.0: +0.11 to +0.26, against encoder
        differences of only 0.015-0.046 and per-fold sigma of 0.071-0.124.
        THE CONTAMINATION IS ~10x THE EFFECT UNDER STUDY.

        Tell: on leaked cases all three encoders score IDENTICALLY (0.293 MAX,
        0.415 TOP-10); on clean cases they diverge (0.080 / 0.114 / 0.125).

        Affects BOTH arms - the fold files are shared static artifacts, so the
        published 0.489/0.512/0.521 carry the leakage too.

        This is a data regeneration, not a code change (folds are committed files).

        TRAP: load_dataset does line[line.index("_")+1 : len(line)-1] and drops
        the last character unconditionally. ANY regenerated fold file MUST end with a
        trailing newline or the last symptom of the last line loses a character.
        (Re-resolved 2026-08-11 (audit): it is cython_utils.py:375. This block
        said :202 and older docs said :184; the file has grown twice since. The
        BEHAVIOUR is pinned by tests/test_characterize_dataset.py, which is the
        thing to trust -- a line number in prose is a comment, not a test.)

        FOLD FILE FORMAT, verified across all 1290 existing lines:
          {HADM_ID}_{raw SYMPTOMS field verbatim}.\n    <- note the APPENDED PERIOD
        Files live at data/folds/Fold{0..9}/{TrainingSet,TestSet}.txt (116/13 lines,
        117/12 for Fold9). Pure LF, zero CR bytes, despite a Windows checkout under
        .gitattributes "* text=auto" - so write with newline="\n".
        Raw file fields (';'-delimited): HADM_ID=0 SUBJECT_ID=1 SYMPTOMS=4 DIAGNOSIS=5.
        Copy field 4 byte-for-byte; do not re-derive it.
        Expect UNEVEN folds after grouping: subject 41976's 15 admissions land in one
        test set, so sizes run ~9-17 rather than a uniform 13.
        Two tests hard-code the current sizes and must change in the same commit:
        test_characterize_dataset.py:114-127 and :158.

        Grouped folds will be uneven in size. That is correct - report the per-fold n.
        Re-measure the 58.1% ceiling afterwards; it was computed on the leaky folds.
        (DONE, 2026-08-06: 76/129 = 58.9%. Noted here 2026-08-11 (audit) because
        this instruction outlived its own completion by five days.)

[x] P3. A4c - UNIFY CROSS-ARM DIAGNOSIS PREPROCESSING   DONE 2026-08-05 (c2115ba)

        It was ONE LINE, and the reason is worth recording because it is not what
        the original note implied. embending_diagnosis (cython_utils.py:234-252)
        embeds preprocess_sentence(description) at :244 but keys the dict by the
        RAW description at :245, and the consumer looks up by raw description.
        So the BERT arm's dict KEYS were already correct - only the text handed to
        the encoder was wrong. Fixing it meant preprocessing the encode input while
        leaving the zip() iterating the original raw list.

        145/145 unique diagnosis descriptions now match across arms, up from 26/145.

        --- original notes, kept for reference ---
        Baseline calls preprocess_sentence on diagnosis text (cython_utils.py:244;
        docs citing :226 are stale); the BERT path did NOT (bert_models.py:292-308,
        cited as :318-332 in older docs).
        119 of 145 (82.1%) unique diagnosis descriptions differed between the arms.
        This broke the project's central design constraint - both arms are supposed
        to share everything EXCEPT the embedding model.

[x] P4. A2 - REPLACE THE COSINE GRADER WITH A DRG-BASED ONE
        DONE 2026-08-06 (75b6530 + the four-arm run).
        Effort: days.  Impact: makes cross-encoder comparison valid AT ALL.
        (State corrected from [~] to [x] 2026-08-11 (audit): the STATUS block
        and the Tier 1 checklist have both said COMPLETE since 2026-08-06, and
        the three items this block listed as outstanding are all reported done
        in the STATUS block above. Only this header was left behind.)

        *** CODE LANDED 2026-08-06. Full writeup: docs/findings/12-drg-grader.md ***

        WHAT SHIPPED
          - PipelineConfig gains `grader` (default "cosine", so LEGACY is
            bit-identical and the golden is untouched). New preset GRADER_DRG,
            selectable as `--pipeline drg` / AICDS_PIPELINE=drg.
          - cython_utils.get_diagnosis_relevance() is the single dispatch point.
            "cosine" delegates to the untouched published grader; "drg-exact"
            returns 1.0 iff any true DRG description equals a predicted one.
          - The two uncalled graders are REPAIRED and live: they now strip the
            prefix before comparing (they used to miss "apr,hcfa:foo" vs
            "hcfa:foo", the same DRG) and no longer divide by zero on empty
            ground truth. Do NOT delete them as dead in P17.
          - All THREE grading call sites wired in one commit: cython_utils :257
            (MAX) and :282 (TOP-K), plus bert_models :575. predictS2V gained a
            trailing config=LEGACY. Splitting this would have graded MAX with one
            ruler and TOP-K with another.
          - 30 new tests in tests/test_drg_grader.py, including a SUBPROCESS
            determinism check across PYTHONHASHSEED 0/1/12345/99991, and the
            exact-match ceiling pinned at 75/129 and 76/129.
          - Both run banners now print the grader. Omitting it is how a run ends
            up filed under the wrong ruler; see P35.

        NOTHING IS STILL TO DO. This block used to carry a three-item "STILL TO
        DO" list -- golden byte-exact "(in flight)", the four-arm rerun under
        drg, and the paired t-test. All three were completed on 2026-08-06 and
        reported in the STATUS block at the top of this file; the list was
        deleted 2026-08-11 (audit) rather than left as a to-do that had already
        happened. Outcomes, so they are not lost with the list: the golden came
        back BYTE-EXACT; all four arms ran under drg; and the t-test is the
        knobless result -- NO PAIR SEPARATES, max |t| 1.718 vs the 2.262 needed.
        That last one IS the question P4 existed to answer, and the answer is
        "no, they do not separate", which is a result rather than a failure.

        MEASURED, and it settles two open questions elsewhere
          - Grouped-fold exact-match ceiling is 76/129 = 58.9% (was never
            measured; closes a P32 item). Legacy is 75/129 = 58.1%, reproduced.
            THE LEAKAGE FIX MOVED RETRIEVABILITY BY ONE CASE - leakage and
            sparsity are independent defects.
          - Per-fold ceiling on the grouped split ranges 4/13 (30.8%) to 13/15
            (87%), which is itself a source of the per-fold variance.
          - Verified by stub run, both predicted in advance: SATURATION IS GONE
            (a binary grader makes the threshold knob inert, so all five
            threshold rows become identical, which also kills the
            threshold-inversion artifact), and DEGENERACY SURVIVES (P==R==F,
            PR=1.0) because it comes from the upstream symptom-pruning gate.

        THE "105 of 145" CORRECTION WAS ABANDONED - IT WAS ALREADY RIGHT.
          An earlier draft of this plan proposed changing it to 85. Both numbers
          are real: 85 is occurrence-level over the 297 raw entries, 105 is
          document frequency over the 224 entries surviving preprocess_diagnosis's
          per-admission dedup, and the 85 are a strict subset. 105 is correct for
          the sentence it appears in, because that sentence bounds exact-match
          RETRIEVAL and a label written twice inside one admission gives a
          retriever no second admission to find it in. Root cause of the gap: 71
          of 129 admissions list their APR label twice byte-identically, an
          upstream DRGCODES artifact. CLAUDE.md and findings/07 now carry an
          explicit "do not correct this to 85" note.

        A PARTIAL-CREDIT GRADER WAS DESIGNED AND REJECTED. Three candidate
          similarity functions were built and measured against the 72 label pairs
          that co-occur on one admission (same episode, so a usable proxy for
          "should score high"). The best cleared 54/72 at threshold 0.6 versus 33
          and 25. It was still rejected: ~156 hand-written lexicon strings mined
          from the same 146 titles it scores, no held-out set, precision 0.13 and
          0.08 on its two lowest rungs, the LOWEST AUC of the three (0.868 vs
          0.902/0.893, differences not significant), structural blindness to
          numbers (ventilator "96+ hours" vs "<96 hours" stuck at 0.900), and it
          graded "discharged alive" against "expired" at 0.900. P4 exists to
          remove an arbitrary ruler; a more elaborate arbitrary ruler concedes the
          argument. There is deliberately NO drg-graded config and from_name
          refuses the name. If revisited, use an EXTERNAL DRG hierarchy, not a
          lexicon derived from these 146 strings.

        ------------------------------------------------------------------
        Original analysis, kept because its warnings were mostly right.
        EVERY LINE NUMBER BELOW IS AS OF 2026-08-05 AND HAS SINCE DRIFTED --
        read them as a record of the reasoning, not as pointers into the tree.
        Current, re-resolved 2026-08-11: the cosine grader is at
        cython_utils.py:518, drg_descriptions at :537, and the two
        encoder-independent graders (which were NOT deleted, as instructed) at
        :576 and :592.
        ------------------------------------------------------------------

        Why: the same encoder both RETRIEVES and JUDGES. The grader is
        get_diagnosis_similarity_by_description_max (cython_utils.py:315), which reads
        the SAME embedding dict used for retrieval. Called from cython_utils.py:126
        and :151 (baseline) and bert_models.py:509 (BERT) - so ALL FOUR ARMS
        SELF-GRADE, not just the transformers.

        A more compressed embedding space grades itself more leniently, "which is
        exactly why BiomedBERT, the most compressed of the three, 'wins' at threshold
        0.9" - 99.71% of its patient pairs clear 0.9 regardless of whether the
        diagnosis is right.

        Enabler: the DIAGNOSIS field already carries DRG group names. String
        comparison needs no embedding.

        *** PARTIAL HEAD START IN THE REPO - BUT NOT USABLE AS WRITTEN ***
        Two encoder-independent graders already exist, ZERO CALLERS:
          cython_utils.py:353  get_diagnosis_similarity_baseline
                               graded - fraction of true diagnoses matched
          cython_utils.py:365  get_diagnosis_similarity_by_drgcode
                               binary  - 1 if any true diagnosis was predicted
        DO NOT let P17's dead-code sweep delete them; they look dead only because
        nothing calls them yet.

        CORRECTED 2026-08-05 - an earlier version of this file called these a
        ready-made head start. They are NOT. Four independent failure modes:
          1. They compare the "apr,ms:" PREFIX as part of the string. `c in list`
             is exact whole-string equality. The real grader at :315 strips the
             prefix first (:321, :323); these two do not. So "hcfa:septicemia
             age >17" and "apr,hcfa:septicemia age >17" - the same DRG - miss.
          2. Prefix ORDER is hash-order nondeterministic. It is built by iterating
             list(set(...)) at :291/:305-307, so "apr,hcfa:foo" vs "hcfa,apr:foo"
             varies BETWEEN PROCESSES. A string-equality grader is therefore
             nondeterministic. PYTHONHASHSEED is not actually pinned.
          3. The prefix is a DRG *type* label (apr/hcfa/ms = three coding systems),
             NOT a DRG code. There is no numeric DRG code anywhere in the data -
             only description text. Cross-system exact matching essentially never
             fires: "Intracranial Hemorrhage" vs "INTRACRANIAL HEMORRHAGE OR
             CEREBRAL INFARCTION" for the same admission.
          4. Prefix reconstruction is itself buggy - `if x in d` at :306 is
             SUBSTRING containment, so "sepsis" inherits hcfa from "hcfa:severe
             sepsis", and description "a" inherits hcfa because "a" is inside the
             literal word "hcfa". Pinned at test_characterize_preprocessing.py:231-273.
        Also: get_diagnosis_similarity_baseline divides by len(gt_diagnosis) with
        no zero guard (:362).
        Still the right starting point. Budget real work, not a one-liner.

        CORRECTED 2026-08-11 (audit): this line used to read "DO NOT use exact
        match alone - see the 58.1% ceiling. Use graded relevance: full credit
        for an exact DRG match, partial for a same-family match." Both halves
        are now wrong. The ceiling is 58.9% (76/129) on folds_grouped -- 58.1%
        (75/129) is the legacy-fold figure, superseded as the headline -- and
        GRADED RELEVANCE WAS DESIGNED, MEASURED AND REJECTED 2026-08-06; see the
        rejection record 70 lines above in this same block, finding 12, and the
        inverted preference order at the ceiling section below. What shipped is
        exact match REPORTED AGAINST THE CEILING, plus the `winnable` (n=76)
        population in RankMetrics.txt. It survived here in imperative voice
        after the rest of the file was corrected, which is exactly the failure
        mode this file's one-state-per-item invariant exists to catch.

[x] P5. B1 - RANK-AWARE RETRIEVAL METRICS (Option C: Recall@K, MRR, nDCG)
        DONE 2026-08-06. Code 47e81bd + 5393cab, four-arm run under --pipeline drg,
        results in docs/findings/13-rank-aware-metrics.md. The analysis below is the
        pre-implementation reasoning, kept because two of its claims were REFUTED by
        the differential test (nDCG is not immune to expanding K; Precision@K's
        min(K,len) is a gaming lever, not abstention protection) and the record of
        being wrong is worth more than a tidied-up version.
        Effort: days.  Depends on P4 (needs the encoder-independent relevance label).
        The ONLY option that fixes all three metric defects at once.

        Why: rank IS computed at bert_models.py:170 and then DISCARDED. The scorer
        (containGreaterOrEqualsValue, cython_utils.py:658 - re-resolved
        2026-08-11 (audit); this block said :390-394 and P36's block said :573,
        both stale, and P36 has since deleted the BERT arm's private copy so
        there is exactly one definition to point at) returns true if ANY of the K candidates clears
        threshold, so a hit at rank 1 and a hit at rank 50
        count identically. That makes score(TOP-50) >= score(TOP-10) true BY
        CONSTRUCTION - verified monotonic in 18/18 model x threshold combinations,
        zero violations. The TOP-K curve in the README is arithmetic, not a finding.

        Bonus: rank-aware metrics are SCALE-FREE, so they are immune to both the
        saturation problem and the per-model calibration problem.


===============================================================================
TIER 2 DETAIL
===============================================================================

[-] P6. B2 - SET-LEVEL SOFT PRECISION / RECALL / F1 (Option B)
        RETIRED 2026-08-11 by owner decision 1. Not done; deliberately
        abandoned, like P18. The proposal, kept so the retirement can be
        argued with:
            soft_precision = mean over predicted p of ( max over true g of cos(g,p) )
            soft_recall    = mean over true g      of ( max over predicted p of cos(g,p) )
            soft_f1        = harmonic mean of the two

        WHY IT IS RETIRED RATHER THAN DEFERRED. Two independent reasons, and
        either alone would be enough.

        1. ITS MOTIVATION WAS DISSOLVED, NOT POSTPONED. This item existed to
           "kill the degeneracy" -- P == R == F in all 12,600 committed BERT
           rows. Findings 04 and 13 established that there was no degeneracy to
           kill: P == R is PR == 1.0, i.e. the answered population and the
           all-cases population being the SAME SET because those arms never
           abstain. RankMetrics.txt reproduces legacy P / R / PR bit-exactly
           from an independent code path, which is the proof. The columns were
           UNLABELLED, NOT WRONG. Implementing a new metric to fix a labelling
           error would be answering the wrong question at the cost of days.
        2. IT WOULD REOPEN A CLOSED FIX. Every term above is a cosine over the
           diagnosis embeddings -- which is precisely the SELF-GRADING that P4
           removed. The arm that produced the retrieval would once again be the
           arm marking it, and a more compact space would once again mark its
           own work leniently. P4's whole argument is that you do not get to
           grade yourself; P6 as specified grades itself in a more elaborate
           way.

        WHAT SURVIVES THE RETIREMENT, and is worth keeping in view: the
        underlying intuition that a prediction SET should be scored against a
        truth SET, so that missing a second true diagnosis costs something.
        Nothing in the project measures that today -- MRR and Hit@K both stop
        at the FIRST relevant candidate. If it is ever wanted, build it on the
        encoder-independent DRG labels, not on cosine, and expect the 58.9%
        ceiling to apply to it.

[x] P7. A4a - "w/o" COLLAPSES TO "w"; NEGATION IS DESTROYED   DONE 2026-08-05 (c2115ba)
        Under CORRECTED, "w/o" is protected before slash-padding and survives as
        "without", so "Tracheostomy w/o Extensive Procedure" and "Tracheostomy w
        Extensive Procedure" are now DISTINCT strings. Under LEGACY both still
        collapse to "tracheostomy w extensive procedure", pinned by
        test_characterize_preprocessing.py as the legacy assertion.

        --- original notes, kept for reference ---
        preprocess_sentence pads "/" so "w/o" becomes ["w","/","o"]; "/" is dropped
        as punctuation and "o" IS AN NLTK ENGLISH STOPWORD.

[~] P8. A4b - COMMA-SPLIT FRAGMENTS               PARTIAL 2026-08-05 (c2115ba)
        The rejoin heuristic landed in split_symptoms behind CORRECTED: a token
        beginning with a space is rejoined to its predecessor. It recovers 80 of
        the 89 fragments - 1805 -> 1725 tokens, 573 -> 564 unique - including all
        26 " organism NOS" occurrences, which were the ones creating spurious 1.0
        matches under mean-of-max.

        IT IS A HEURISTIC AND IT IS INCOMPLETE. See P27 for the remaining nine.

        --- original notes, kept for reference ---
        Symptoms are ","-split but ICD-9 short titles contain commas. Because
        similarity is mean-of-max, any two admissions sharing a contentless
        fragment get a spurious 1.0 contribution. Severity distinctions also
        collapse (stage I vs stage IV).

[x] P9. B5 - KEEP THE LEGACY NUMBER          SATISFIED BY P1's SEAM
        (State corrected from [ ] to [x] 2026-08-11 (audit) to match the Tier 2
        checklist entry, which has said SATISFIED since 2026-08-05. There is no
        commit for this item and there should not be: it was satisfied by a
        design decision taken elsewhere, which is a legitimate way for an item
        to close and the reason the STATUS block no longer tries to give every
        DONE item a SHA.)
        Report the existing MAX-at-0.6 result alongside everything new, clearly
        labelled as legacy, so results stay comparable to Comito et al. and to this
        project's own history. Keep every metric SIDE BY SIDE rather than replacing.
        HOW IT WAS SATISFIED, and why this is stronger than what was asked for:
        the ask was to COPY a legacy number into reports beside the new ones. P1
        instead made LEGACY a permanent, selectable, byte-reproducible PATH --
        `--pipeline legacy`, default everywhere, pinned by the golden. A copied
        number can only be trusted; a re-runnable path can be checked.

[ ] P10. A5 - CENTRE THE EMBEDDINGS BEFORE COMPUTING COSINE
        DEMOTED 2026-08-11 to DIMINISHED PAYOFF (optional, off the publication
        path) by owner decision 2. Not retired -- it is still the only probe of
        anisotropy this project has, and anisotropy is a documented property of
        the models. What changed is the size of the prize: centring can only
        rehabilitate the COSINE-GRADED tables, and the headline path is
        drg-exact, where there is no cosine for a shared threshold to be
        meaningful ON. It buys a better version of a ruler the project stopped
        leading with.
        "ONE LINE" IS A TRUE STATEMENT ABOUT THE SUBTRACTION AND A FALSE ONE
        ABOUT THE WORK. Centring MOVES THE NUMBERS, so by P1's rule it cannot
        ride an existing pipeline name: it needs its own PipelineConfig field
        with a legacy-preserving default, its consumer and banner line in the
        same commit (P35), and a 42-53 minute golden gate. Budget that, not one
        line.
        Effort: ONE LINE of arithmetic, plus a config seam and a golden run.
        Impact: unknown, worth measuring.
        BERT embeddings are anisotropic (Ethayarajh 2019) - a documented property of
        the MODELS, not of MIMIC-III. Demo with random vectors: a shared offset moves
        mean pairwise cosine from 0.001 to 0.94, reproducing the three models'
        measured similarity profiles with no medical content whatsoever.
        Fix: subtract the mean embedding (the core of BERT-whitening). If it works, a
        single shared threshold becomes meaningful again -- on the cosine tables.
        NOT THE SAME as normalize_embeddings=False, which cannot affect results
        at all: L2-normalising before a cosine that already divides by the norms
        is algebraically a no-op. (Re-resolved 2026-08-11 (audit): this said
        bert_models.py:337-343 and there are now TWO such sites, :352-358 and
        :261-267. The no-op argument holds at both, which is the point -- a
        second copy of a red herring is still a red herring.)

[ ] P11. B4 - PER-MODEL THRESHOLD CALIBRATION (Option D)
        DEMOTED 2026-08-11 to DIMINISHED PAYOFF by owner decision 2, and it is
        the weakest of the three. LIKELY MOOT: drg-exact makes the threshold
        knob inert -- all five threshold rows collapse to one value -- so on the
        headline path THERE IS NO THRESHOLD LEFT TO CALIBRATE.
        Set each threshold at a fixed PERCENTILE of that model's own similarity
        distribution instead of a hardcoded 0.6. Equal-selectivity values from the
        measured medians: BiomedBERT ~0.93, Bio_ClinicalBERT ~0.84, BlueBERT ~0.72.
        A patch, not a fix - makes cross-model comparison meaningful without
        addressing leniency or rank.
        TWO HAZARDS THAT MAKE IT MORE EXPENSIVE THAN IT LOOKS.
          1. HIGHEST GOLDEN-RISK SURFACE OF THE THREE DEMOTED ITEMS. The
             thresholds are duplicated across SEVEN set literals whose SET
             ITERATION ORDER decides output row order, plus a hand-kept matching
             list at bert_models.py:511 that a comment is the only thing keeping
             in sync. Touching them moves formatting, and the golden compares
             bytes, not floats.
          2. ITS EVIDENCE SOURCE IS UNFIXED. The measured medians above come
             from analyze_score_distributions.py, which is P37: it re-implements
             the grader against its own numpy cosine and reads no
             PipelineConfig. Calibrating to legacy-measured percentiles would
             pin a new knob to a retired ruler. Fix P37 first or do not start.

[ ] P12. B3 - MEAN INSTEAD OF MAX (Option A)
        DEMOTED 2026-08-11 to DIMINISHED PAYOFF by owner decision 2, and it is
        the best-shaped of the three because it does not replace anything.
        Cheap. Fixes only the leniency defect. New problem: dilution - a patient with
        3 true diagnoses and 1 correct prediction averages the correct match against
        2 irrelevant comparisons, so patients with more diagnoses are penalised for
        having more diagnoses. Worth having as a REGISTERED AGGREGATOR, not an end
        state: MAX stays, MEAN sits beside it, and a reader compares the two rather
        than being handed one.
        TWO DUTIES COME WITH IT.
          1. SORT BEFORE REDUCING. A mean over hash-ordered labels reintroduces
             last-ULP nondeterminism -- finding 12 hit exactly this on HADM
             178513 -- and no existing test would catch it, because the values
             agree to 15 significant figures and differ in the printed digits.
             MAX is immune (it selects rather than accumulates), which is why
             this hazard is NEW with MEAN rather than pre-existing.
          2. IT IS A NEW CONFIG AXIS, so P35's rule binds: the field, its
             consumer, its SUPPORTED_*-style refusal for unknown values, and its
             banner line all land in the SAME commit. A named aggregator nothing
             reads is the P4 mistake with a different noun.


===============================================================================
THE 58.9% CEILING - READ BEFORE DESIGNING ANY EXACT-MATCH METRIC
===============================================================================
REWRITTEN 2026-08-11 (audit). This section was the single most misleading thing
in the file: it was titled 58.1%, still said the figure had not been re-measured,
and still listed GRADED RELEVANCE as preference #1 -- a design this project
subsequently built, measured, and REJECTED. A reader following it would have
redone rejected work. What it got right, and why it is still here first: the
ceiling itself is real and binds every exact-match metric anyone designs.

The method predicts by copying an answer from a retrieved patient, so the answer must
EXIST in the pool to be copied. 145 unique diagnosis descriptions across 129
admissions; 105 of them (72.4%) occur exactly once in the whole dataset.
(105 is the admission-level count, and it is the RIGHT one here -- do not
"correct" it to the occurrence-level 85. See P4's block.)
Only 76 of 129 test cases (58.9%) have their correct DRG present anywhere in their
fold's training pool ON folds_grouped; 75 of 129 (58.1%) on the legacy folds. The
other ~41% are unwinnable under exact matching - not because retrieval is bad, but
because the answer is not in the library.
A PERFECT RETRIEVER THEREFORE CAPS AT 58.9% UNDER EXACT MATCH.
Per fold the ceiling ranges from 4/13 (30.8%) to 13/15 (86.7%) on the
canonical split (finding 14 corrected the 3/12 figure, which described the
environment-dependent Windows split), which makes it a
source of per-fold variance in its own right.

BOTH FIGURES ARE PINNED IN tests/test_drg_grader.py. Cite the tests, not this
paragraph: a number in prose rots, a number in a test cannot.

Three ways to handle it. The preference order INVERTED once they were measured:
  1. Graded relevance - partial credit for a same-family DRG. *** WAS RANKED
     FIRST; DESIGNED, MEASURED AND REJECTED 2026-08-06 (finding 12,
     metric-redesign.md). *** Three candidate ladders were built; the best
     needed ~156 hand-written lexicon strings mined from the same 146 titles it
     scores, with no held-out set, precision 0.13 and 0.08 on its two lowest
     rungs, the LOWEST AUC of the three, blindness to numbers ("96+ hours" vs
     "<96 hours" scored 0.900), and it graded "discharged alive" against
     "expired" at 0.900. P4 exists to remove an arbitrary ruler; a more
     elaborate arbitrary ruler concedes the argument. If ever revisited, use an
     EXTERNAL DRG hierarchy, not a lexicon derived from these 146 strings.
  2. Report against the ceiling - "X of a possible 58.9%". ADOPTED.
  3. Restrict evaluation to the 76 winnable cases and say so. ADOPTED TOO -- it
     is RankMetrics.txt's `winnable` population, reported beside all-cases and
     answered rather than instead of them.

NOTE, corrected 2026-08-11 (audit): this line used to read "measured on the
CURRENT, LEAKY folds. Re-measure after P2. Neither plan says so." All three
clauses are now false. It was re-measured (2026-08-06), and BOTH plan files say
so -- correctness-fixes.md:206-210 and metric-redesign.md:168-183.
(The metric-redesign anchor was written as :141-156, which was true of the
pre-audit file and was falsified by this same audit's +49-line edit to it --
:141-156 now lands in the unrelated nDCG blockquote. Anchor by content, not by
line, when citing across files edited in one pass.)


===============================================================================
TIER 3-5 DETAIL  (refactor and infrastructure; these do NOT move the numbers)
===============================================================================

[ ] P13. C11 - MINT A BASELINE-ARM GOLDEN
        tests/golden/ covers the BERT/stub path ONLY. cython_utils.py is SHARED
        between the arms, so a change there could move the baseline numbers with every
        test still green - exactly the failure mode the golden exists to prevent.
        Now mintable, because the arm finally runs. Linux only; needs the 21 GB model
        and ~13 minutes of compute.

        THE COST ESTIMATE ABOVE IS INVALIDATED BY C4, in the cheap direction
        (noted 2026-08-11 (audit)). "Linux only, 21 GB, pod time" was true when
        the baseline executed at import with no seam. C4 gave it
        run_analysis(encoder=None, config=LEGACY, out=None), so THE BASELINE ARM
        NOW TAKES AN INJECTED ENCODER exactly as the BERT arm does -- which
        means a StubEncoder-based baseline golden is mintable LOCALLY, on
        Windows, with no sent2vec build and no 21 GB download. That is the
        version worth having first: it is what would catch a shared
        cython_utils.py change moving the baseline numbers, and that is the
        failure mode this item exists to prevent. A REAL-MODEL baseline golden
        still needs Linux and the 21 GB model, but it can ride the pod session
        P29-runs and P40 need anyway -- and the decision-3 legacy verification
        run is already a reference candidate for it.
        THE PREMISE IS UNCHANGED AND STILL TRUE: cython_utils.py is SHARED
        between the arms and only one arm has a golden.

[x] P14. C10 - RESULT PROVENANCE            DONE 2026-08-11 (5a52d26)
        Was: no output directory recorded the commit, machine, or library versions
        that produced it.

        SHIPPED: aicds/runs.py write_run_metadata(), called by BOTH arms after the
        PerformanceIndex handle closes -- the same after-the-close discipline as
        RankAccumulator.write, and for the same reason. test_golden.strip_trailer
        raises "No timing trailer found" if that file lacks its trailer, so
        anything that could throw before the trailer is written surfaces as A
        GOLDEN FAILURE THAT LOOKS LIKE THE PIPELINE CHANGED. Writing it last also
        means its presence proves the run reached the end.

        The payload is more than four lines because a name with no fields is the
        sibling of the P4 mistake: it records the pipeline NAME (config.name_of)
        AND fold_dir / preprocess_version / grader, so an edited registry still
        leaves a true record and `name` degrades to null rather than to a lie.
        Plus git SHA + dirty (both null, not False, when git cannot be asked),
        fold_dir_sha256, model key + label, K_FOLD, and platform/python/numpy.

        THE FOLD DIGEST IS ALLOWLISTED TO Constants.TRAIN/TEST, and that was not
        the first attempt. Hashing every file under the directory pulled in a
        hidden 105-byte desktop.ini that Windows keeps in every data/folds/Fold*/
        and git has never tracked, so the byte-identical legacy split hashed to
        b7554079... on the owner's Windows checkout and 93f0943a... on the Linux
        pod -- and this project's whole story is Windows dev to RunPod Linux. It
        failed the other way too: Explorer rewrites desktop.ini when a folder view
        changes, so an untouched split would start reporting itself as a different
        one. Silently, in the one field whose entire job is saying whether two runs
        saw the same data. With the allowlist, local now matches the predicted
        Linux digest 93f0943a...

        CONSUMER LANDED IN THE SAME COMMIT, as P35's rule requires:
        compare_models.resolve_pipeline prefers run_metadata.json and demotes
        dirname inference to a [WARN] fallback.

[x] P15. C2 - UNIFY OUTPUT PATHS AND THE FIVE GLOBS   DONE 2026-08-11
        Writer half 9d08c94, reader half 7927a88.

        Was: five discovery sites disagreed four ways about the base directory,
        AND the baseline wrote "Prediction Output_" with a SPACE, so NO glob in
        the repo matched baseline output at all. Fix direction was FORCED by
        test_golden.py:115's hard-coded \d{8}_\d{2}-\d{2}-\d{2}.

        SHIPPED: src/aicds/runs.py owns both halves. run_dirs() gives the flat
        cwd layout for out=None (frozen by the golden) or ROOT/{key}/{stamp}/ for
        --out ROOT. discover() has exactly ONE rule -- a directory is a run iff it
        contains PerformanceIndex.txt -- with no marker= parameter, because making
        the marker configurable would turn "this run predates rank metrics" and
        "this directory is not a run" into the same silence. It REFUSES rather
        than skips a PerformanceIndex.txt in neither layout: skipping is how a run
        vanishes from a comparison table without anyone noticing.

        TWO EXCEPTIONS SURVIVE AND ARE NOT DRIFT. tests/test_golden.py keeps its
        own tmp-dir glob and its timestamp regex -- that regex is what forced the
        underscore direction, and the golden must not depend on the code it
        audits. test_reorganization.py:75 keeps a recursive ** because it asserts
        that committed results exist at all, not that a run can be discovered.

        See docs/findings/10-output-path-fragmentation.md, which now carries a
        dated Closed section.

[x] P16. C1 - DELETE THE stop_words MONKEYPATCHES    DONE 2026-08-11 (7400eff)
        THE COUNT WAS 11. This line used to say 8 and the roadmap said 4; both
        were wrong, and the four nobody had counted were in NON-TEST code
        (bert_models, baseline_sent2vec, bert_eval, analyze_score_distributions).
        All eleven were the identical set(stopwords.words('english')), which is
        what made removal provably inert -- the patch always assigned the value
        cython_utils had already computed. It feeds tokenization -> embeddings ->
        every number, so it got its own commit with nothing else in it and its
        own golden gate: BYTE-EXACT, 52:35.

[x] P17. C4 - DEAD CODE + THE LAST 2 BASELINE DEFECTS   DONE 2026-08-11
        Dead code 94b4e24; baseline 9c9e251. The baseline half was VERIFIED
        2026-08-11: the pod's --pipeline legacy --out run byte-matched
        results/baseline/05082026_18-55-32 exactly (353,418 bytes to the
        trailer cut) -- C4's handle collapse and C5's --out plumbing proven
        content-preserving in one shot.

        THE OLD DESCRIPTION OF THE TWO HANDLES WAS WRONG, AND INSTRUCTIVELY SO.
        This file said ":279 opened 'w' and never closed, :420 a second handle
        'a' to the same path", which reads as though the 'w' handle was opened
        and abandoned. It was not. THE 'w' HANDLE WROTE THE ENTIRE BODY. What it
        never got was an explicit close: rebinding performance_out_file to the
        'a' handle dropped the last reference to the 'w' object, so CPython
        flushed and closed it right there -- BEFORE the trailer's first write,
        which O_APPEND then placed at the true end of file. The emitted sequence
        was [everything written through 'w'][trailer], which is exactly what one
        with-block produces. It worked by refcount accident, not by design, and
        establishing that is WHY collapsing the pair to a single with-block is
        byte-safe rather than merely tidier. Same path, same 'w' truncation, same
        encoding and newline translation, same write order, trailer still last,
        and generate_timing_pdf's try/except still inside the block.

        Also shipped: run_analysis(encoder=None, config=LEGACY, out=None) and a
        __main__ guard that passes from_env() explicitly, so
        AICDS_PIPELINE=corrected python -m aicds.models.baseline_sent2vec keeps
        working -- a bare run_analysis() would have defaulted to LEGACY and
        silently dropped the variable on the one entry point with no flag.

        cython_utils' two encoder-independent graders were KEPT, as P4 required.
        entity/{Admission,Symptom,Drgcodes} and their test_reorganization.py
        import were deleted in the same commit, as this item said they must be.

[-] P18. C3 - SALVAGE hf_automodel BEFORE DELETING bert_eval.py
        RETIRED 2026-08-10 by owner decision 2; executed as a plain deletion in
        5ca7f64 (bert_eval.py and the entire evaluation/ package).

        The salvage instruction stood in three documents for months and pointed
        at a target that NEVER EXISTED IN ANY BRANCH: git log --all
        --diff-filter=A is empty for encoders/ and hf_automodel*. The GPU-aware
        line it was meant to preserve is worthless given finding 08 (a GPU buys
        under 0.5% for these arms), and mean-pooling can be rebuilt behind the
        P38 encoder registry if it is ever actually wanted. Deleting 545 orphaned
        lines also removed the third ensure_nltk_data/format_time copy.

        CORRECTED 2026-08-11 (audit): this paragraph used to end "...and one of
        the 11 stop_words patches", crediting C2's deletion with removing it.
        THAT IS WRONG. `git show 7400eff` -- C1, the commit before -- deleted
        ALL ELEVEN, bert_eval.py:63 included, with the deliberate note "line
        only; the file itself is deleted next commit". C1 removed the patch; C2
        removed the file it had sat in. The distinction is not pedantry: C1 was
        the commit that got its own golden gate BECAUSE stop_words feeds
        tokenization -> embeddings -> every number, and the whole point of
        isolating it was that the gate covers all eleven removals and nothing
        else. Attributing one of them to a later commit would put a
        number-touching change outside the gate that proved it inert. The count
        of 11 stated at the top of this file and in P16 is correct as written.
        (Decision 2 at the head of this file predicted C2 would delete the
        patch. It reads verbatim as a decision record; the executor simply took
        all eleven in one gated commit instead, which is strictly better and
        changed nothing about the outcome.)

[ ] P19. C6 - A REAL CLI                         MOVED TO P38 by decision 1
        There is no main.py and no [project.scripts]. bert_models.py:87 still
        calls input("Select model [1-3, Q]: "). NOTE it is DEAD on the supported
        path -- run_bert_analysis.py always passes --model and select_model()
        returns before prompting -- but live via
        python -m aicds.models.bert_models. Needs a stdin-closed test over every
        subcommand.
        THE MECHANISM, recorded 2026-08-11 (audit) because "dead but live" is
        the kind of statement that gets simplified into a wrong one: select_model
        RETURNS EARLY when it is given a model id, so the input() is unreachable
        whenever --model is passed, and run_bert_analysis.py always passes it.
        The ONLY way to reach the prompt is `python -m aicds.models.bert_models`,
        whose __main__ calls run_analysis() with model_id=None.
        CROSS-REF P41: that same `-m` entry point is the one that silently
        ignores AICDS_PIPELINE. So the module's __main__ has two independent
        defects, both invisible from the supported path, and a real CLI closes
        the class rather than either instance.

[x] P20. C7 - ONE PerformanceIndex PARSER REPLACING FOUR   DONE 2026-08-11
        1f69e11 added src/aicds/analysis/performance_index.py and proved it
        reproduces all FOUR private parsers (compare_models, analyze_performance,
        build_dashboard_data, build_readme_plots) on the committed goldens BEFORE
        anything was deleted. 7927a88 then deleted them, rewired five consumers,
        and converted the equivalence test into pinned literals in
        tests/test_performance_index.py -- the old form compared new against old,
        which stops meaning anything once old is gone.

        The count in the original line ("three") was already stale when written;
        compare_models had added a fourth. The FS-vs-F1 divergence between the
        two near-identical clones is preserved as an explicit choice rather than
        an accident of which clone a caller reached.

[ ] P21. C5 - SentenceEncoder PROTOCOL + ENCODER REGISTRY
        embed_sentence(text) -> seq[seq[float]], with readable names and 1|2|3 aliases.
        This is also the seam the RAG work needs.
        CONSTRAINT ADDED 2026-08-11 (audit): any registry built in the P38 repo
        MUST be built on aicds.runs.MODEL_KEYS / KEY_LABELS, not on a fresh
        name table. Those two are now the single authority on display-name <->
        directory-key, and C7 collapsed a genuine four-way naming drift onto
        them; runs.discover RAISES on an unregistered label rather than
        skipping it. A second, independent name map in the encoder registry
        would recreate exactly the drift C7 removed, in the repo that is
        supposed to be the clean one.

[ ] P22. C8 - FIX THE DASHBOARD (it 404s today)
        Regenerate into dashboard/public/data/, commit it, delete the stray
        dashboard/dashboard/ tree.
        STEP 1 OF 3 IS ALREADY DONE (recorded 2026-08-11 (audit)): C7's gate
        re-ran build_dashboard_data.py and the JSON came out BYTE-IDENTICAL
        (sha 0638b6c5...), which is both the regeneration and the proof that
        rewiring the builder onto runs.discover changed nothing. The
        regenerated JSON is sitting UNTRACKED in the working tree right now, at
        the correct dashboard/public/data/ path. Steps 2 and 3 -- commit it,
        delete the stray dashboard/dashboard/ tree -- are deferred to P38 with
        the rest of the dashboard work, because whether that JSON gets committed
        at all is a P38 data-exposure question, not a dashboard question.

[ ] P23. C9 - CONVERT scripts/ TO DEPRECATION SHIMS
        MOVED TO P38 by decision 1, and added to P38's carry list 2026-08-11
        (audit) -- decision 1 moved it with the rest of the polish but the P38
        checklist entry never named it.
        NOT COSMETIC ANY MORE, and this changed after decision 1 was taken.
        scripts/ now holds SAFETY MECHANISMS, not just argparse: check_out_root's
        DUA guard (C5) refuses an un-ignored --out root before the model loads,
        and the run banners are the only record of which config a run used for
        an arm that writes to a timestamp-only directory. Shimming scripts/
        therefore means RELOCATING a safety mechanism, and a shim that forwards
        the call while dropping the guard would be a silent regression of the
        one defect class that cannot be taken back.

    P24. C12 - PER-CASE OUTPUT FILES ARE ALL EMPTY
        RETIRED 2026-08-11 (audit) -- the checkbox lives in the RETIRED section
        above, and the work lives in P40. This block is kept because its
        MEASUREMENTS are still the best statement of the defect:
        the baseline emits 258 zero-byte files; cython_utils.py:203-204 opens the two
        handles and :308-309 closes them with NOTHING written in between. The BERT arm
        never opens them but still creates the Fold*/ dirs (bert_models.py:539-540).
        Neither arm has EVER produced per-case output. (Line numbers re-resolved
        2026-08-11.)
        P40 answers P24's question ("implement or delete") with a third option:
        write a NEW SIBLING FILE and leave the dead handles dead. Repairing them
        would put new writes inside the region the byte-exact golden covers, for
        output nothing reads.

[ ] P25. C13 - FOLD PARALLELISATION
        The 10 folds are embarrassingly parallel and the loop is 93%+ of runtime.
        Use multiprocessing, NOT threads - the loop is pure-Python arithmetic, so the
        GIL serialises it. Do NOT vectorise with numpy: pairwise/SIMD summation changes
        the last bits and breaks the byte-exact golden.

[ ] P26. RETRIEVAL INTERFACE -> LOCAL DECODER  (the RAG endgame)
        The pipeline is ALREADY retrieve-then-predict, so this is a short conceptual
        hop: what is missing is a clean retrieval interface (P21) plus a generation
        step. A HOSTED LLM API IS RULED OUT - the repo carries MIMIC-III under a
        PhysioNet DUA, so the decoder must run locally. This is where a GPU finally
        earns its keep; for the encoder arms it buys under 0.5%.
        ITS PREREQUISITE NOW LIVES IN ANOTHER REPO, which nothing said (noted
        2026-08-11 (audit)). P21 -- the retrieval interface this item is waiting
        on -- MOVED TO P38 by decision 1. So P26 is a CROSS-REPO dependency: the
        RAG endgame cannot start in this repo, because the seam it needs will be
        built in the clean one. Worth knowing before anyone schedules it here.


===============================================================================
NEW ITEMS - DISCOVERED WHILE IMPLEMENTING TIER 1 (added 2026-08-05)
===============================================================================

[x] P27. FINISH THE COMMA REJOIN - DONE 2026-08-11 (7e49212)
        The measured fix landed as `corrected2` exactly as this block planned:
        1716 / 561 / ZERO on the real data, firing 9 times across the 4 labels.
        One deviation from the sketch below, found by adversarial verify and
        worth keeping: the shipped predicate also requires the accumulated
        predecessor to start UPPERCASE (the ICD-9 short-title tell), because
        the 24-char cap alone would have joined a bare lowercase pair like
        "fever,cough" -- the cap does NOT prevent that, contrary to what this
        block's first draft assumed. And the dataset counts were measured to be
        BLIND to a widening of the rule (caps 25-40 change nothing), so the
        predicate is pinned separately from the counts. The rest of this block
        stands as the plan that was executed.

        IT LANDS AS A THIRD preprocess_version, working name `corrected2` --
        owner decision 4, 2026-08-11. NOT as an edit to `corrected`. The reason
        is reproducibility rather than caution: `corrected` is what produced
        every existing results tree, every number in findings 11 and 13, and the
        byte-compare reference the pod session will use, so changing it in place
        would silently invalidate all of them and leave no way to reproduce the
        old figures. The config seam was designed for exactly this -- add a
        field value with a legacy-preserving default, never change an existing
        one. `corrected` stays FROZEN; corrected2 is corrected plus rule 2.
        A CONSEQUENCE WORTH PLANNING FOR: the three tests that broke under the
        naive rule pin `corrected`, so they must STAY GREEN. corrected2 gets its
        own pinned expectations -- including proof that "fever,cough" still
        SPLITS under corrected2, since the 24-char cap is what is supposed to
        prevent the over-join that reverted the first attempt.

        P8's rule ("a token starting with a space rejoins to its predecessor")
        recovers 80 of 89 fragments. It misses the nine where the intra-label comma
        has NO trailing space, so the fragment does not start with a space:
            'stage NOS'      x4
            'stage III'      x3
            'uncomp'         x1
            'pharyngoesoph'  x1
        These were confirmed by an adversarial verifier AFTER an earlier version of
        this file claimed the rule was exact. It is not. The docstring on
        split_symptoms now says so.

        A rule-2 extension (also rejoin a token that is a known continuation
        fragment) was BUILT AND THEN REVERTED. Reverted because a naive form -
        "rejoin any part with no leading space" - OVER-JOINS: a legitimate
        unspaced symptom list like "fever,cough" collapses to one token, which
        broke 3 tests. Editing those tests to accommodate an unverified change is
        the exact antipattern to avoid.

        MEASURED OUTCOME OF THE CORRECT VERSION: 1716 tokens, 561 unique, ZERO
        residual fragments. So the target numbers are known in advance.
        The correct form needs a bounded rule - e.g. rejoin only when the part is
        one of the four known continuations, or when the combined string matches a
        real ICD-9 short title - not a general no-leading-space rule.

        WORTH KNOWING: CMS caps ICD-9 short titles at 24 characters, which is WHY
        the truncated forms 'uncomp' and 'pharyngoesoph' exist at all. That cap is
        a usable oracle for a bounded rule.

[x] P28. GUARD make_folds.py --out                    DONE 2026-08-05 (930c162)
        check_out_dir() refuses two hazards behind two flags:
          --force-legacy  writing into data/folds at all
          --overwrite     writing into any dir that already holds Fold*/ dirs
        Path comparison is realpath+normcase, so data/folds_grouped/../folds is
        caught too. The two guards COMPOSE rather than short-circuit: data/folds
        is both protected and populated, so replacing it takes both flags.
        The second guard matters more than it looks - fold dirs beyond the new
        --n-splits are NOT removed, so regenerating with a smaller --n-splits
        silently leaves a MIXTURE of two runs on disk.
        tests/test_make_folds_guard.py, 13 tests. Suite 122 -> 135 passed.
        Also pins the determinism the gitignore rests on: two runs byte-identical.

    P29. THE TWO ONE-CHANGE-AT-A-TIME CONFIGS
        SPLIT INTO TWO ITEMS 2026-08-11 (audit), because one P-number in two
        states is what let this sit as [x] here, [~] under PARTIAL and [ ] in
        the ranked checklist simultaneously -- three states for one item:
          [x] P29-configs  DONE 2026-08-05 (45f36fc)
          [ ] P29-runs     OPEN -- eight runs, 4 arms x 2 configs, on the pod

          folds-only       grouped folds, legacy preprocessing -> leakage effect
          preprocess-only  legacy folds, fixed preprocessing    -> preproc effect

        preprocess-only STILL LEAKS PATIENTS by construction. It is an attribution
        instrument; never report a number from it as a result.

        --pipeline's choices now derive from config.PIPELINE_NAMES instead of a
        hand-kept list, so a config cannot exist-but-be-unselectable. That was a
        real bug here: c2115ba shipped the seam with no CLI able to reach
        CORRECTED, and 31bea66 had to add the plumbing afterwards.

        TO DO (P29-runs): EIGHT runs -- 4 arms x 2 configs -- to split the
        measured 0.090 baseline F1 drop into a leakage part and a preprocessing
        part. (CORRECTED 2026-08-11 (audit): this line said "run both on the
        baseline arm", i.e. two runs, which contradicted the eight-run scope
        recorded under PARTIAL at the top of this file and the eight runs the
        staged script actually performed. Two baseline runs would attribute the
        BASELINE's drop and leave the three BERT arms unattributed, which is
        half the experiment. Eight is the number.)

[x] P30. WIDEN .githooks/pre-commit                   DONE 2026-08-05 (d77de02)
        Both rules had missed data/folds_grouped/ - verified, not assumed:
          rule 1 ^data/(raw|folds)/ did not match "folds_grouped"
          rule 2 scored those files ZERO, because fold lines are bare
                 "{6-digit id}_{symptoms}." with no literal HADM_ID token

        rule 1 is now ^data/(raw|folds)[^/]*/ and rule 2 also counts lines
        matching ^[0-9]{6}_, taking the larger of the two counts.

        Both were needed. A TestSet.txt holds only 15 IDs, BELOW the threshold of
        20, so the content rule alone would not have stopped it - the path rule
        does. Same defence-in-depth reasoning that added rule 2 originally.
        Verified end to end: staging a grouped fold file now exits 1.

[~] P31. THE GOLDEN TAKES 42-53 MINUTES, NOT 20-26
        MOSTLY DONE 2026-08-11 (C9). REWRITTEN TO RESIDUE-ONLY 2026-08-11
        (audit); state corrected from [ ] to [~] to match the Tier 3 checklist
        entry, which has said MOSTLY DONE since C9 landed.

        MEASURED SEVEN TIMES, not the twice this block used to claim: 42:28,
        43:28, 43:50, 44:32, ~50:00, 52:35, 53:10. Call it 42-53 minutes.
        (42:28 arrived 2026-08-11 with the D3+D4 gate and moved the range's
        floor -- the third time a new measurement has invalidated a count or
        range in these docs, which is the argument for quoting the range
        loosely and the list exactly.)

        WHAT IS LEFT ON THE RANGE IS EXACTLY ONE LINE: tests/test_golden.py:68
        still says "about 20 minutes". C9 corrected the RANGE in every other
        statement of the number -- CLAUDE.md, this file, revival-roadmap.md,
        findings/10, architecture.md, setup.md, README.md, pyproject.toml's
        golden-runtime comment -- and left that one ALONE ON PURPOSE.
        test_golden.py is the binding constraint on the entire refactor;
        editing the file that holds the safety net in order to fix a docstring
        is not a trade worth making. That is why this item is [~] and not [x],
        and why it should probably stay [~] forever: the residue is a
        deliberate choice, not an oversight.

        NARROWED 2026-08-11 (audit), then CLOSED the same day: this block used
        to claim C9 corrected EVERY other statement, leaving one line. That was
        true of the range and false of the COUNT -- C9 wrote "five times" plus
        a five-item list omitting 43:50 into CLAUDE.md, README.md,
        docs/guides/setup.md, docs/plans/revival-roadmap.md and pyproject.toml.
        The commit after the audit fixed all five, so the residue is back to
        exactly ONE statement: test_golden.py:68's "about 20 minutes",
        deliberate, protected by the binding constraint. Do not restate any
        wider claim without re-grepping for `five times`.

        STILL WORTH DOING while in the area: tests/stubs.py records
        StubEncoder(dim=64) at 128s versus 1245s at 768 - worth stating as the
        DEVELOPMENT loop, with 768 reserved for minting anything committed.

[x] P33. compare_models.py SILENTLY PICKS ONE RUN PER MODEL, UNLABELLED
        CLOSED 2026-08-11 (7927a88 + 5a52d26); recorded here 2026-08-11 (audit).
        Found 2026-08-05 while deciding where to put the corrected results.
        Belonged with P14 (provenance) and P15 (paths) -- and was in fact closed
        BY them, which is why it had no commit of its own and was missed.

        BOTH CONSEQUENCES ARE CLOSED, and the remedy this block proposed was
        superseded by a better one:
          1. UNLABELLED PIPELINE -- gone. run_metadata.json records the pipeline
             by name AND by all three PipelineConfig fields;
             compare_models.resolve_pipeline prefers it and demotes dirname
             inference to a [WARN] fallback; a MIXED-PIPELINE TREE IS REFUSED
             outright rather than half-charted; and the pipeline is printed on
             the PDF cover page. The --results-dir separation is still available
             but is no longer the only thing keeping the trees apart.
          2. STRING-SORTED TIMESTAMPS -- gone, but NOT by the fix proposed
             below. runs.discover picks the run whose directory has the NEWEST
             MTIME rather than parsing the stamp at all, and PRINTS the choice
             when a key has more than one run. The DDMMYYYY hazard is written
             down where the code is, at runs.py:497-498, with this item's own
             example (15022026 sorts after 05082026 while being five months
             older). Parsing the stamp would have re-derived a date the
             filesystem already knows; mtime cannot drift out of sync with the
             directory it describes.
        The "Fix:" line below is kept as the ORIGINAL PROPOSAL, superseded.

        discover_runs() takes candidates[-1] - the LEXICALLY last timestamp per
        model - and records nothing about which PIPELINE produced it. Two
        consequences:

          1. Dropping corrected runs into results/ alongside the legacy ones
             makes the legacy runs VANISH from the PDF with no warning, and the
             PDF does not say which pipeline it charted. A reader cannot tell.
             Worked around for now by keeping the trees separate via
             --results-dir; that is a workaround, not a fix.
          2. Timestamps are DDMMYYYY_HH-MM-SS and sorted as STRINGS, so the
             ordering is not chronological across months. 31012026 sorts after
             01022026 although it is a day earlier. It happens to be correct
             today (05 Aug < 06 Aug) which is exactly why it will go unnoticed.

        ORIGINAL PROPOSAL, superseded in part: parse the timestamp into a real
        date before sorting, record the pipeline in run_metadata.json (P14), and
        put it on the PDF. Two of the three shipped as written; the timestamp
        parse was replaced by mtime.

[~] P32. COLLECT THE CORRECTED-PIPELINE RESULTS         PARTIAL 2026-08-06 (3b8aed9)
        DONE: all four arms ran on the pod, exit 0, ~48 min total (baseline
        ~13 min, each BERT arm ~11.5 min). Downloaded to
        results_corrected/<model>/<timestamp>/, GITIGNORED under the same rule
        as results/ - the run dirs carry all 129 HADM_IDs and the pre-commit
        hook blocks them. Aggregates written up in docs/findings/11, which
        carries no identifiers.

        HEADLINE AS FIRST WRITTEN -- SUPERSEDED, DO NOT QUOTE. Kept only so
        the correction below has something to point at:
        | at MAX threshold 1.0 - the only setting where nothing saturates -
        | the spread across the four encoders COLLAPSES from 0.038 to 0.009.
        | Effectively a four-way tie, with the 700D sent2vec baseline
        | nominally on top. Most of the measured "difference between encoders"
        | was leakage, exactly as finding 05 predicted before these runs
        | existed.

        STILL TRUE from that block: baseline TOP-10 @ 0.6 drops 0.4824 ->
        0.3922, so about a fifth of the published 0.489 was contamination; and
        saturation and degeneracy BOTH SURVIVE untouched, expected since they
        are metric design rather than data splitting.

        CORRECTED 2026-08-06 (e83b5be + this commit): the "spread collapses,
        four-way tie" headline above was MAX-only and has been walked back in
        finding 11 and in the STATUS block at the top of this file. See there
        for the replacement claim (ordering inverts with K; TOP-K rewards not
        abstaining). Do not re-quote the four-way-tie framing from anywhere.

        STILL TO DO -- PRUNED TO TWO ITEMS 2026-08-11 (audit). Three of the five
        that used to be listed here were already done, and both remaining ones
        are RUNS rather than code:
          - P29-runs: folds-only and preprocess-only, to attribute the drop
          - P37: re-run analyze_score_distributions.py -- the saturation
            statistics (mean pairwise cosine 0.72-0.93, the >=0.6 percentages)
            are LEGACY-measured and labelled as such in the README, which is a
            stopgap rather than a fix. Note this one is not a plain re-run: the
            script re-implements the grader (P37), so fixing it comes first or
            the new numbers come from the same fourth cosine implementation.

        THE THREE THAT WERE DELETED FROM THIS LIST, and why each was false:
          - "regenerate the comparison PDF -- BLOCKED by P34". NOT BLOCKED. P34
            was fixed 2026-08-06 (b53024e): SANITY_CHECKS_BY_PIPELINE keys the
            expectations by pipeline, C7 re-ran all 16 checks green on
            results/, results_corrected/ and results_drg/, and the PDFs exist on
            disk. This line outlived its own blocker by five days.
          - "update docs/README.md". Done -- refreshed in 2e5c6f3, and the C9
            documentation pass went over it again.
          - "re-measure the 58.1% ceiling". Done 2026-08-06: 76/129 = 58.9% on
            the grouped folds, pinned in tests/test_drg_grader.py.

[x] P34. compare_models.py SANITY CHECKS WERE PINNED TO LEGACY VALUES
        DONE 2026-08-06 (b53024e). Found 2026-08-06 while trying to build the
        corrected comparison PDF. State corrected from [ ] to [x] 2026-08-11
        (audit) -- the STATUS block has listed P34 as DONE since b53024e landed,
        and this block was the last of FIVE places that disagreed about it.

        WAS: 16 baked-in assertions hard-coded legacy numbers, so
        --results-dir results_corrected failed all 16 and exited before writing
        the PDF. The checks were RIGHT to fire; the expectations were simply
        legacy-specific.

        SHIPPED, and it is now this file's own example of a check that refuses
        rather than guesses: SANITY_CHECKS_BY_PIPELINE keys the 16 expectations
        by pipeline name, and resolve_pipeline decides which set applies --
        explicit --pipeline first, then the runs' own run_metadata.json (P14),
        then the directory name with a [WARN]. An unknown tree, a pipeline with
        no recorded expectations, and a run in which ZERO checks executed are
        all HARD EXITS. C7's gate re-ran them on all three committed trees:
        16/16 pass on results/, results_corrected/ and results_drg/ bare, and
        results_p5 REFUSES bare because it is not in _PIPELINE_BY_DIRNAME --
        that refusal being the feature, not a gap.
        The assertions were never deleted to unblock the PDF. They are still the
        only thing verifying the parser reads the columns correctly.

        THE HOST LABEL WAS FIXED IN THE SAME COMMIT, and better than this block
        asked. It said "hosts_by_date maps 15022026 and 05082026 only". That is
        no longer true in two ways: HOSTS_BY_DATE now carries 06082026 ->
        "RunPod Linux" as well, and an UNMAPPED date resolves to "unknown host"
        instead of falling through to a per-model constant. The old fallback
        asserted "M-series Mac" for every arm, so it did not degrade gracefully
        -- it stated a specific wrong machine. An honest blank beats a confident
        error in a provenance box.

        REMEMBER when writing up numbers from these trees -- CORRECTED
        2026-08-11 (audit): this block used to warn that the corrected numbers
        were "STILL SELF-GRADED (P4) and still rank-blind (P5)". BOTH WERE
        CLOSED ON 2026-08-06, hours after this was written. What remains true,
        and is the version to carry: the corrected numbers are leakage-free,
        preprocessing-unified, encoder-independently graded and rank-aware --
        and they still do not support an encoder ranking, because NO PAIR
        SEPARATES (max |t| 1.718 vs 2.262). Say that explicitly rather than
        letting a reader infer a winner; the README already made one overclaim
        of exactly this kind and it had to be corrected.

[ ] P35. NO CONFIG AXIS MAY BE UNANNOUNCED, AND NONE MAY LACK A READER
        Effort: minutes for the remaining part. Found 2026-08-06 while doing P4,
        by making the mistake this item now guards against.

        For a window during development, `--pipeline drg` was selectable and
        NOTHING read config.grader. The run succeeded, the banner looked right,
        it produced a full 10-fold result set, and every number in it came from
        the COSINE grader while being labelled DRG. Nothing failed, and nothing
        downstream could have detected it.

        DONE: config.SUPPORTED_GRADERS + require_supported_grader() raise at
        startup, before the model load, on any grader value with no consumer.
        Both arms call it. Both run banners now print the grader, and the
        baseline prints its pipeline at all for the first time -- it writes to a
        timestamp-only directory, so the run log is the ONLY record of config.

        DONE 2026-08-11: run_metadata.json (P14) landed in 5a52d26 -- and it
        landed WITH its consumer in the same commit (compare_models prefers it
        over the dirname), which is this item's own rule applied to itself.

        STILL TO DO:
          - The same audit for any FUTURE field. The rule: a config field and
            its consumer land in the SAME commit, and the field appears in the
            banner in that commit too.
          - P41, found 2026-08-11 (audit), is a NEW INSTANCE OF THIS ITEM'S
            FAILURE MODE in a place the rule did not look: not a field with no
            reader, but an ENTRY POINT that never asks. `python -m
            aicds.models.bert_models` imports no from_env, so AICDS_PIPELINE is
            silently ignored there while the baseline twin honours it. Same
            outcome as the original P4 mistake -- a run whose config is not
            what its invocation said -- reached by a different route. The rule
            this suggests: when an arm gains a config entry point, EVERY entry
            point of EVERY arm gets checked in that commit.

[x] P36. bert_models KEPT A PRIVATE containGreaterOrEqualsValue AND NO TEST
        TOUCHED IT
        DONE 2026-08-06 (1d68df9). State corrected from [ ] to [x] 2026-08-11
        (audit) to match the checklist entry, which has said DONE since the
        commit landed.
        Found 2026-08-06 during P4's call-site sweep.

        WAS: bert_models.py defined its own copy while cython_utils.py held the
        original. tests/test_characterize_similarity.py imported the cython_utils
        one; tests/test_bert_symptom_pairwise.py defines a THIRD and injects it.
        So the copy the BERT arm actually called in production was exercised by
        nothing.

        SHIPPED. The copy is DELETED -- bert_models.py:197-198 is now a comment
        recording that it used to be there and what its docstring wrongly
        claimed -- and the production call site at bert_models.py:681 is
        qualified `util_cy.containGreaterOrEqualsValue`, so the BERT arm reaches
        the shared definition. The one remaining definition is
        cython_utils.py:658. (Line numbers re-resolved 2026-08-11 (audit); this
        block said :198 and :573, and P5's block said :390-394. All three were
        written at different points in the file's growth.)
        AND IT IS PINNED, which is what stops it recurring:
        tests/test_topk_threshold_gate.py:58-59 asserts bert_models has NO
        attribute of that name, with a failure message explaining why a private
        copy is a silent divergence. A test that the copy cannot come back beats
        a test of the copy.

        Why it mattered: P5 (rank-aware metrics) reads this function's answer --
        it says "did anything in the top K clear the bar", and rank-awareness
        needs "at what rank". Change cython_utils with a copy still in place and
        the baseline gets the new rule while the BERT arm keeps the old one. The
        golden would have caught the numeric fallout without naming the cause.
        (The "do this BEFORE P5, not during" instruction that used to close this
        block was FOLLOWED -- 1d68df9 landed 2026-08-06, before P5's 5393cab --
        and was deleted 2026-08-11 (audit) because an instruction that has been
        carried out reads as one that has not.)

[ ] P37. analyze_score_distributions.py RE-IMPLEMENTS THE GRADER
        Effort: hours. Found 2026-08-06 during P4's call-site sweep.
        PRIORITY RAISED 2026-08-11 (audit) -- see the C7 note below.

        :148-183 simulates get_diagnosis_similarity_by_description_max against
        its own numpy cosine -- a fourth cosine implementation in this repo --
        and imports no PipelineConfig. (Line drift corrected 2026-08-11 (audit)
        from :152-187; the block is compute_per_patient_max_scores.) It feeds
        build_dashboard_data.py and build_readme_plots.py, so it will keep
        generating SATURATION EVIDENCE FOR A RETIRED RULER: under drg-exact
        there is no saturation to show.

        WHY IT IS MORE URGENT THAN IT WAS. C7 fixed build_readme_plots.py, which
        used to raise FileNotFoundError on EVERY invocation. So this script's
        legacy-measured statistics are no longer inert prose that happens to be
        stale -- they are now ACTIVELY REGENERATED into committed SVGs by a
        script that runs. The failure mode changed from "a stale number sits in
        a document" to "a stale number is re-derived and re-committed on
        demand", and the second is the one that survives a documentation pass.
        It is also P11's evidence base and P32's last open code item.

        Its spot_check_against_cython guards only the cosine kernel, not the
        aggregator or the prefix stripping, so it passes while being wrong.

        Fix: have it call get_diagnosis_relevance with an explicit config, and
        label its output with which grader produced it. The README already had
        to mark its statistics "legacy-measured" as a stopgap.

[ ] P38. CODE-ONLY PUBLIC REPO, CLINICAL DATA REMOVED
        Effort: days, mostly sequencing rather than code. Added 2026-08-06.
        BLOCKS PUBLICATION. Full investigation: docs/plans/public-release.md.

        Why: this repo is PUBLIC and carries committed MIMIC-III records under a
        PhysioNet DUA that forbids redistribution. A paper citing it points
        reviewers -- and PhysioNet -- straight at that. It is also just wrong to
        leave in place.

        DECIDED 2026-08-06: a NEW CLEAN REPO, not a history rewrite. Rewriting
        does NOT remove data from GitHub -- pushed objects stay reachable by SHA
        through the UI and API until Support garbage-collects them, forks hold
        independent copies, and clones persist. A rewrite would look like
        remediation without being it.

        Exposure, measured 2026-08-06: 29 tracked files carry identifiers. 25
        substantive (data/raw, the 20 fold files, 3 committed run outputs, the
        golden) and 4 incidental prose mentions. The data entered at commit 2 of
        53 (b23657b) and was NEVER modified since; each clinical path was
        touched by exactly one commit.

        THAT AUDIT HAS DRIFTED IN BOTH DIRECTIONS, re-dated 2026-08-11 (audit).
        It is a snapshot, and the tree moved under it:
          - "commit 2 OF 53" no longer locates anything: the repo is past 86
            commits. Read it as "the second commit ever made", which is the
            durable fact.
          - ONE CARRIER WAS REMOVED. a53c84b untracked
            .system-design-visualization.md (a Feb-2026 snapshot that had been
            committed despite declaring itself local-review-only).
          - TWO WERE ADDED, by the refactor itself. C6 and C7 introduced the
            real sample line HADM_ID=124073 into
            src/aicds/analysis/performance_index.py:27 (a docstring showing the
            per-case header trap) and tests/test_performance_index.py:880 (a
            pinned parser literal). Neither is under data/, so neither was
            caught by the path rule; both are single IDs, so neither trips the
            20-distinct-ID content rule. They are being replaced with a
            synthetic 999001 in the commit after this one.
          - THE TREND IS THE POINT, and it is the argument for doing P38 rather
            than a one-off scrub: NEW CODE RE-INTRODUCES THE LINE. Documenting a
            parser trap naturally reaches for a real example, and the real
            examples are all identifiers. A cleanup that is not accompanied by a
            convention (synthetic IDs in fixtures and docstrings) will be undone
            by the next person writing a docstring.

        TWO MEASUREMENTS MAKE THIS NON-LOSSY, which is why it is worth doing
        properly rather than just deleting things:

          1. THE GOLDEN SURVIVES. It carries all 129 HADM_IDs, but
             test_golden.py:155 already has canonicalise() normalising the
             trailer and run paths before comparing. Extend it to mask the ID
             token and commit a masked golden: every TP/FP/P/R/FS/PR value and
             every formatting quirk it exists to catch survives, because THE
             IDENTIFIERS ARE LABELS, NOT MEASUREMENTS. No pipeline change, so
             the existing golden proves the change is inert.
          2. FOLD DEFINITIONS SHIP WITHOUT IDENTIFIERS. Measured: all 1290 fold
             rows are EXACTLY raw_symptoms + "." -- uniform, zero exceptions
             across both fold sets. So membership can ship as index lists into a
             canonical ordering of data/raw and reconstruct byte-for-byte.
             load_dataset needs a small change; the golden gates it.

        THE GAP THAT CANNOT BE ENGINEERED AWAY: reconstruction needs to know
        WHICH 129 admissions, that list is itself identifying, and the selection
        was made upstream with no criterion recorded anywhere in this repo.
        Preferred route is publishing the derived subset on PhysioNet as a
        credentialed-access supplement with GitHub code-only. Fallback is to say
        plainly that the public repo reproduces the METHOD but not the exact
        numbers.

        ORDER (1-5 are ordinary reversible work here; 6 makes a new repo; 7 is
        irreversible and needs explicit instruction):
          1. Mask identifiers in canonicalise(); re-mint the masked golden.
             *** USEFUL ON ITS OWN AND SAFE TODAY *** -- removes 129 identifiers
             from the most awkward file in the repo whatever else is decided.
          2. data/folds -> index lists; adjust load_dataset; golden byte-exact.
          3. Mask the three committed Prediction_Output_* files.
          4. Write the MIMIC-III reconstruction recipe and VERIFY it against the
             schema -- the current one is inferred from the data's shape
             (ADMISSIONS + DIAGNOSES_ICD/D_ICD_DIAGNOSES.SHORT_TITLE + DRGCODES),
             not from the original authors' code.
          5. Scrub the 4 incidental mentions.
          6. Build the new repo from the resulting tree, fresh initial commit.
             Confirm zero identifiers with the scanner used for the audit, and
             confirm .githooks/pre-commit passes with NO --no-verify anywhere.
          7. Retire this repo (private, or delete). IRREVERSIBLE.

        PREREQUISITE OUTSIDE THE CODE: publishing MIMIC-derived work needs a
        credentialed PhysioNet account with the DUA signed and CITI training
        done. If that is not in place it precedes all of the above.

        RELATED, now that publication is a goal: P29's attribution runs and P5's
        rank-aware metrics both move from "nice" to "a reviewer will ask", and
        P14 (run provenance) is how you answer "which pipeline produced this
        table".

[ ] P39. THE TWO ARMS BREAK SCORE TIES DIFFERENTLY
        Effort: ~an hour of code + one 43-min golden run. Found 2026-08-06 while
        implementing P5. NOT urgent -- measured impact is below the effect size --
        but it is a cross-arm divergence with no defensible reason to exist.

        The two arms rank candidates with different sorts:
          BERT     bert_models.py:177  similarities.sort(key=..., reverse=True)
                   Python's sort is STABLE -> ties break by training-set order
          baseline cython_utils.py:278  np.argsort(-1 * similarity_array)
                   defaults to quicksort, which is NOT stable -> ties break
                   arbitrarily, and not reproducibly across numpy versions

        Hit@K barely notices, because it only asks whether ANY of the top K
        cleared the bar. MRR reads the rank directly, so a tie at the front
        changes the score.

        MEASURED, all 129 real test cases, StubEncoder at 768D, drg-exact labels:
          tied pairs (any score tie)                    83
          test cases with any tie                       81 / 129
          tied pairs where relevance DIFFERS             1   <- the only ones
          cases where MRR depends on tie order           1 / 129
          ties at the rank-1 MAX pick                    1 (order-sensitive: 0)

        Ties are COMMON but almost never consequential: two candidates tie when
        they share the winning symptom for every test symptom, which is
        structural and encoder-independent, but they then usually carry the same
        relevance, so no order can change a metric.

        IMPACT BOUND. The rank-1 tie is not order-sensitive, so the one affected
        tie sits at rank >= 2, giving an RR delta <= 1/2 - 1/3 = 0.1667 and an
        MRR shift <= 0.0022 on the 76-case winnable population. Inter-arm deltas
        measured on the same runs are 0.0015-0.0067, so this is at the bottom of
        that range rather than above it. A rank-1 order-sensitive tie WOULD have
        mattered (0.0066, squarely inside the range) -- worth re-measuring if the
        data or the encoder ever changes.

        CAVEAT ON THE MEASUREMENT: it used StubEncoder, so the specific ties are
        not the ties a real encoder produces. What transfers is the mechanism and
        the order of magnitude, not the count of 1.

        FIX: np.argsort(..., kind="stable") so both arms break ties identically.
        That MOVES NUMBERS, so it goes behind the config seam as a `ranking`
        field defaulting to legacy, exactly like preprocess_version and grader --
        and it needs its own golden run. Do NOT fold it into an existing pipeline
        name silently; a reader must be able to tell which ranking produced a
        table.

        Worth doing because "the arms are comparable" should be true by
        construction, not by an impact bound that happens to be small this time.

--------------------------------------------------------------------------------
[~] P40  PER-CASE OUTPUT - CODE DONE 2026-08-11 (efa3794); the RUN remains
     RankCases.txt ships from RankAccumulator exactly as sketched below -- a
     projection of state the accumulator already held, written after the close
     by both arms, consumer in the same commit. Golden gate at 7e49212 was
     byte-exact (42:28). What remains is the pod's 4-arm drg run to produce
     the first RankCases-bearing trees and actually execute the
     self-selection test; the baseline's file is the load-bearing one.
     Found 2026-08-06 while writing up P5. Checkbox added and cross-linked
     from TIER 2 on 2026-08-11 (audit): this block existed for five days with
     no entry in the ranked checklist at all, which is how the file's declared
     top item stayed invisible to anyone reading the checklist. It supersedes
     P24 (retired).

     WHY IT IS SUDDENLY VALUABLE. It has been on the known-defects list for
     months as a cosmetic wart. P5 turned it into the binding constraint on the
     one question P5 raised and could not answer.

     THE QUESTION. P5 found that how you treat abstention reorders all four
     arms. The `answered` population (abstentions excluded) puts BioSentVec
     FIRST; `winnable`/`all-cases` (abstentions score 0) put it LAST. Every
     sign inverts. So the obvious doubt about `answered` is SELF-SELECTION: the
     baseline chose which 98 of 129 cases to answer, so those 98 may simply be
     the easy ones, and comparing its MRR on 98 self-chosen cases against the
     BERT arms' 129 is not a matched comparison.

     THE TEST THAT ANSWERS IT. Compute each BERT arm's MRR restricted to the
     baseline's 98 answered cases, and compare against its MRR on all 129. If
     the restricted figure is higher, those cases ARE easier and `answered`
     flatters the baseline by a measurable amount. That is a matched
     comparison, and it is the only way to put a number on the confound.

     THE TRAP - DO NOT USE THE RATIO. MRR_all-cases / coverage looks like it
     bounds the effect and returns ~1.0, which reads as "confound is 0.5%, move
     on". It is ALGEBRAICALLY IDENTICAL to MRR_answered: abstentions contribute
     0 to the all-cases numerator, and coverage is exactly the denominator
     ratio, so the quotient is MRR_answered by construction and carries zero
     information. This was computed, believed for about a minute, and withdrawn
     on 2026-08-06. The warning is now in analyze_rank_metrics.py's docstring
     and in finding 13, because it is convincing enough to fool a careful
     reader twice.

     WHY IT IS BLOCKED. The test needs the per-case relevance vector for each
     (arm, HADM_ID). RankMetrics.txt carries only per-fold and 10-FOLD
     aggregates. The per-case files that would carry it are ALL EMPTY:
       cython_utils.py:203-204 opens the two handles
       cython_utils.py:308-309 closes them
       ... with NOTHING written in between
       bert_models.py:539-540  never opens them at all, yet still mkdir's Fold*/
     (line numbers re-resolved 2026-08-11; they had drifted ~140 lines)
     The baseline emits 258 zero-byte files. Neither arm has EVER produced
     per-case output.

     SCOPE, and the reason it is not a golden risk: write a NEW sibling file
     (RankCases.txt or per-case rows inside RankMetrics.txt) rather than
     repairing the two dead handles. PerformanceIndex.txt stays untouched, so
     the byte-exact golden still covers exactly what it covered before -- the
     same reasoning that made P5 additive. Repairing the dead handles is a
     separate, lower-value cleanup -- and P24, the item that used to carry it,
     is RETIRED on exactly this reasoning rather than left open.
     THE SIBLING-FILE PATTERN NOW HAS TWO SHIPPED PRECEDENTS (noted 2026-08-11
     (audit)): RankMetrics.txt (P5) and run_metadata.json (P14). Both are
     written AFTER the PerformanceIndex handle closes, both left the golden
     byte-exact, and both were additive by construction rather than by careful
     editing. Follow the same call site and the same after-the-close discipline;
     it is a paved road now, not a proposal.

     DATA-USE CONSTRAINT: per-case output is keyed by HADM_ID, so it is
     DUA-covered and must stay under results*/ (gitignored by glob) and out of
     docs/. Only aggregates go in findings. The pre-commit hook's 20-distinct-
     HADM_ID rule is the second line of defence, not the first.
     PARTIALLY ENFORCED IN CODE SINCE C5 (noted 2026-08-11 (audit)):
     runs.check_out_root asks git whether the --out root is ignored and RAISES
     UnignoredOutRoot before the model loads if it is not, probing a
     representative per-case path rather than the root. So "must stay under
     results*/" is now checked rather than merely written down -- for runs that
     pass --out. It is not total: a run with out=None returns immediately (that
     is what keeps the golden contract untouched), and nothing stops a human
     copying a RankCases.txt into docs/ afterwards. Treat the guard as the
     floor, not the ceiling.

     REPORTABLE EITHER WAY. If the 98 are no easier, `answered` stops being
     suspect and the baseline's first place there becomes the honest headline.
     If they are easier, that quantity IS the confound and belongs in the
     README. There is no outcome in which this measurement is wasted.

--------------------------------------------------------------------------------
[x] P41  `python -m aicds.models.bert_models` SILENTLY IGNORES AICDS_PIPELINE
     DONE 2026-08-11 (8965546), the very next commit after the audit, exactly
     as this block demanded. Found by the C1-C9 audit; a P35-class hole. The
     -m path now resolves via from_env() with the baseline twin's comment.

     THE DEFECT. bert_models.py's `__main__` calls a bare `run_analysis()`,
     which takes `config=LEGACY` by default, and the module imports no
     `from_env`. So:
         AICDS_PIPELINE=corrected python -m aicds.models.bert_models
     runs the LEGACY pipeline, prints a banner saying legacy, files its output
     as legacy -- and never mentions that a variable asking for something else
     was set. Nothing fails. This is the P4 failure mode exactly: a run whose
     configuration is not what its invocation asked for, with no signal.

     WHY IT IS WORSE THAN A PLAIN OVERSIGHT: IT IS ASYMMETRIC BETWEEN THE ARMS.
     The baseline's twin entry point at baseline_sent2vec.py:543-552 DOES honour
     it, deliberately, with an eight-line comment explaining this precise
     failure mode -- "a bare run_analysis() defaults to LEGACY and would have
     silently dropped the variable on the one entry point that has no
     --pipeline flag to fall back to". The reasoning was written down, applied
     to one arm, and not carried to the other. So the two arms disagree about
     what the same command means, in a project whose central design constraint
     is that the arms share everything except the embedding model.

     HOW IT SURVIVED. It is invisible from the supported path: run_bert_analysis.py
     always passes --pipeline explicitly, so the `-m` entry point is the only
     way to reach it -- the same entry point that is the only way to reach the
     dead input() prompt (P19). C4 gave both arms a __main__ guard; only one of
     them got from_env(). A guard that runs the wrong config is not obviously
     better than no guard.

     FIX: give bert_models' __main__ the same `config=from_env()` parity, with
     the comment that explains why, so the next person to read either module
     finds the same reasoning. NOT ON THE GOLDEN PATH -- test_golden.py injects
     the config through run_analysis directly and never reaches __main__ -- so
     it needs the fast suite, not a 42-53 minute gate. Say that in the commit
     message rather than leaving a reader to wonder why no golden ran.
